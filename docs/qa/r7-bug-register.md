# R7 QA Bug Register — CC · SM · Agent · UIs · APIs

QA campaign 2026-08-25 (branch `r7-demo-hardening`). Mode: fix-as-found
(D16); real Keycloak JWT in scope (D17). Every bug: ID, layer, severity
(BLOCKER/MAJOR/MINOR), status (OPEN / FIXED@commit / WONTFIX-reason /
UNTESTABLE-reason). Source `audit` = found in the 2026-08-25 readiness
audit (docs/designs/production-demo-readiness.md); source `qa` = found by
this campaign's live testing.

## Register

| ID | Layer | Severity | Bug | Status |
|---|---|---|---|---|
| QA-001 | Deploy/CC+Console | BLOCKER | CC & Console boot with no DB schema on Postgres (runtime create_all is sqlite-only; alembic entrypoints referenced by nothing, wrong paths, alembic assets not COPY'd into images) | FIXED@2588c29 — entrypoints run `alembic upgrade head` (fail loudly), alembic.ini COPY'd into both images; verified live: fresh boot, CC/Console healthy with REAL DB probes |
| QA-002 | Console UI | BLOCKER | UI unloginable: no VITE_KEYCLOAK_URL/REALM build args; realm default `harkeniq` vs shipped `harkeniq-platform`; auth URL built against empty origin | FIXED@2588c29 — VITE_KEYCLOAK_URL/REALM/CLIENT/DEV_MODE build args (realm harkeniq-platform); browser click-through pending UI QA phase |
| QA-003 | Deploy | BLOCKER | Default stack has no agent service (commented out); uncommented block exits 3 on TLS validation (no `HARKENIQ_SITE_MANAGER_TLS:"false"`) | FIXED@2588c29 — agent in default stack w/ SITE_MANAGER_TLS false; verified live: registered + OBSERVING at SM |
| QA-004 | Deploy | BLOCKER | Host-port collisions: postgres publishes 5432 (dies on hosts with local postgres — verified); `network-sim` profile double-binds 50051 | FIXED@2588c29 — postgres host publish removed (opt-in comment); switch-sim on host 50052 |
| QA-005 | Console+CC backend | BLOCKER | No real auth: secure mode raises HTTP 501; insecure mode grants `platform_super_admin` to every caller | FIXED@2588c29 — real JWKS validation (harkeniq/security/oidc.py) in Console (dual-realm) + CC (single-realm; configure_auth finally CALLED); compose runs INSECURE=false; verified live: real Keycloak token accepted at both, no/garbage token 401; 11 unit tests |
| QA-006 | SM UI/API | BLOCKER | Approver identity self-asserted (free-text input → `decided_by`); audit proves nothing | FIXED@c8e1beb — CC approvals attribute the authenticated JWT email through RouteApproval+audit; SM-local decisions recorded as sm-local:<name>, empty actor 422, UI disables buttons until named |
| QA-007 | Agent CLI | BLOCKER | `harken diagnose` crashes (TypeError: `_TARGET_COLLECTIONS` values called as functions; wrong `engine.evaluate` signature); no Nagios exit codes; zero test coverage | FIXED@82308a2 — 4 crashes fixed, Nagios exit codes 0/1/2/3, one-shot debounce + honest confidence; 5 e2e subprocess tests |
| QA-008 | Agent demo | BLOCKER | Trending slope uses wall-clock hours under compressed demo time → `-5,402,706 RPM/hr … 0 hours`; "46 hours before failure" unproducible | FIXED@0bfdac3 — narrative clock (1 poll = 1 narrative hour) via SkillEngine time_fn; live output now `-170.6 RPM/hr ... in 44 hours`; doc-09 -200 RPM rate + 90d guardrail restored |
| QA-009 | SM UI | BLOCKER | LLM explanation computed/stored/served but rendered by no UI (Incident TS type lacks `explanation`) | FIXED@074d851 — DIAGNOSIS panel (summary/confidence/action/reasoning) on parent + child incidents; SM UI build green |
| QA-010 | Deploy | MAJOR | No restart policies on any service; CC/Console healthz hardcoded ok (no DB probe); depends_on = service_started only | FIXED@2588c29 — restart policies everywhere; CC/Console healthz now probe the DB (503 when degraded); service_healthy deps; two stale runtime tests updated |
| QA-011 | Deploy | MAJOR | No .dockerignore: 374MB contexts; host node_modules overwrite image installs (darwin-on-linux breakage) | FIXED@2588c29 — root .dockerignore (contexts ~374MB -> small; node_modules overwrite eliminated) |
| QA-012 | Deploy | MAJOR | Agent identity/baselines unpersisted in compose (re-enrolls each restart) | FIXED@2588c29 — named volumes for agent + network-agent /var/lib/harkeniq |
| QA-013 | Deploy | MAJOR | Compose omits Console↔CC linkage env (tenant id, console URL/API key) — marketplace sync + usage reporting silently disabled | FIXED@2588c29 — CC tenant/console/keycloak env linkage restored in compose |
| QA-014 | Docs | BLOCKER | No README/quickstart/DEMO runbook/seed data (README = 22 bytes UTF-16) | FIXED@2054b57+ — README, DEMO.md runbook, seed-demo.sh; ALL runbook steps executed live this session (inject path corrected to /test/inject-fault) |
| QA-015 | Security | MAJOR | Checked-in creds: Keycloak admin/admin (temporary:false), postgres harkeniq/harkeniq, SM token dev-token-sm | FIXED@47ce04a — all 10 compose credential sites env-overridable (${VAR:-labdefault}) + .env.example; lab defaults documented in DEMO.md §7 |
| QA-016 | Agent | MAJOR | Peer HMAC secret defaults ""; empty accepted → forgeable heartbeats | FIXED@5606244 — peers + empty heartbeat.secret is a startup config error (R-X15) |
| QA-017 | Agent | MAJOR | Agent silently downgrades to plaintext when tls:true but tls_ca empty | FIXED@5606244 — reporter refuses tls:true without tls_ca (was silent plaintext downgrade) |
| QA-018 | CC | MAJOR | CC→SM gRPC unconditionally plaintext (no TLS option in SMClient) | FIXED@38b3d7f — SMClient takes tls_ca (CCConfig.sm_tls_ca / HARKEN_CC_SM_TLS_CA); all 6 channel sites TLS-capable; plaintext only when no CA configured |
| QA-019 | Console | MAJOR | License signing key auto-generated into DB; `license_signing_key_path` never read; CC never verifies any license | FIXED@38b3d7f+fd44675 — Console half (earlier): license_signing_key_path honored, DB autogen lab-only. CC half (now): `harkeniq_cc/license.py` verifies the Console-issued `.lic` at startup (`license_key_path` finally has a reader + new `license_verify_key_path`); cross-package roundtrip test locks CC verify to Console sign. Posture: tampered/wrong-tenant/unreadable → refuse start; EXPIRED → grace posture w/ loud error (delinquency is Console-enforced; R-H7 forbids auto-disable); unconfigured secure → loud warning (compose lab). Site registration uses the VERIFIED license fingerprint (mismatched body 400s); compose/env plumbing added; 12 tests |
| QA-020 | Agent | MAJOR | Autonomy chain unwired: preconditions/lease-gate/budget/blast-radius/verification never called by Agent or executor; four R3a actions have no Redfish executor branch | FIXED@1bc56b4 — `_execute_gated` runs the chain on BOTH execution paths (approved loop + SM directives): preconditions (fail-closed on unobservable facts until QA-024) → lease gate (stop switch/expiry hard-deny; class/budget "propose" satisfied by the carried approval) → blast radius → execute → budget consume + blast record → deferred verification (UNKNOWN producible, audit-chained `verified:*`); AgentBudgetEnforcer+BlastRadiusLimiter instantiated, lease mirrored into budget on every heartbeat ack; 4 Redfish branches (SEL_CLEAR Dell SEL/HPE IML, BMC_RESET GracefulRestart, POWER_CYCLE, POWER_CAP_ADJUST w/ read-back verify) + simulator routes; 15 gate tests + 7 executor tests |
| QA-021 | SM | MAJOR | SuppressionEngine + SMAutonomyEnforcer never instantiated; leases carry defaults (unlimited budget, stop_switch=False) | FIXED@1bc56b4 — deeper than registered: AgentIdentityService itself was never constructed (production leases were EMPTY bytes) and its keypair loader ran sqlite-only SQL against the agent's table. Now: `sm_settings` KV table (migration 0003) persists the SM keypair; make_state builds identity+enforcer+suppression; Heartbeat leases carry real budgets/stop_switch/suppression domains + policy-granted action classes w/ risk ceiling; ReportAction COMPLETED draws down site budgets; PushPolicy applies CC policy + audit-chains stop-switch flips (was log-and-ack); suppression wired into correlation on_onset (subsystem→family, per fault domain) + sweep auto-recovery; `/api/autonomy` state/stop-switch/re-enable endpoints (named actor, audited); 13 wiring tests |
| QA-022 | CC | MAJOR | Stop switch = in-process dict; never persisted, never reaches SM/lease | FIXED@1bc56b4 — `cc_stop_switch` table (migration 0003) + StopSwitchRepo replace the dict; SMClient.push_policy (was missing entirely); flip pushes to ALL sites immediately + fleet poller re-converges each cycle (heals SM restarts); budget upsert/delete also push. Budget→policy mapping (documented in policy_push.py, **needs Vinod ratification**): only device_type="*" rows map; level 0/1 grant nothing, level 2 grants SEL_CLEAR+BMC_RESET, level 3 adds POWER_CYCLE/POWER_CAP_ADJUST/CONFIG_RESTORE; high-risk (FIRMWARE_*, INTERFACE_*) NEVER budget-granted — keep their dedicated approval paths; 7 tests. Also fixed live: SM `sites` get_or_create insert race (savepoint) surfaced by per-heartbeat lease DB work |
| QA-023 | Agent | MAJOR | RedfishProtocol.execute_action self-grants full ActionType allow-list (bypasses R-X6 for any direct caller) | FIXED@7589a4c — protocol takes `allow_list` at construction (default = R1 set, never full ActionType); agent passes its configured actions.allow_list; 7 tests (refusal, factory passthrough, agent wiring) |
| QA-024 | Agent | MAJOR | OS signals package (~900 lines) + log-poll loop unwired; SEL/IML never reaches a verdict; log_cursors dead | FIXED@bdce53f — `os_signals` config section (enabled/interval, default on); sources auto-register only when their backing exists (syslog path, dmesg/journalctl/smartctl binaries — containers stay quiet); `_log_signals_loop` maps error/warning OS events to verdicts (corroborating posture: error caps at WARNING except mce/nvme/disk_io channels) and polls SEL/IML via the never-called `Poller.poll_logs` on `polling.log_interval`; id-SET cursor (not high-water — survives SEL clear) persisted in the previously-dead `log_cursors` table; new-entry verdicts ride `_report_changed_verdicts`; forwarding sets `sel_events_forwarded` so the A2.1 SEL_CLEAR precondition can now actually pass. OS-heartbeat absence still has no observer (stays fail-closed for POWER_CYCLE). 14 tests |
| QA-025 | Agent | MAJOR | resources monitor unwired; `HARKENIQ_RESOURCES_PROFILE` env silently discarded (no `resources` config section) — D12 unenforced | FIXED@ac92100 — `resources` config section exists (HARKENIQ_RESOURCES_PROFILE finally works), ResourceMonitor instantiated + _resource_loop wired (degradation ladder stretches poll cadence); 3 wiring tests |
| QA-026 | Services | MAJOR | Structured JSON logging + request-id middleware imported only by tests; all services use basicConfig text | FIXED@ac92100 — configure_logging (JSON + HARKEN_LOG_PLAIN escape) in all three service mains; X-Request-Id middleware mounted in all three apps |
| QA-027 | Demo/fixtures | MAJOR | Fixtures diverge from doc 09/11: 4 fans vs 8, 1 disk vs 4 (drive_1..3 routes 404), CPU2 sensor absent | FIXED@ac92100 — dell_r750 fixtures at doc 09/11 baseline: 8 fans (9200-10400), 4 drives (life 96/92/85 + original), CPU2 Temp 52C; _find_drive made deterministic (Id/Name first, Model falls back to lowest bay) |
| QA-028 | Demo | MAJOR | Doc 09 scenes missing: cross-subsystem correlation, thermal scene inert (Exhaust +2°C vs 75°C threshold), summary dashboard is text blob, `--scenario` absent, PSU action masked by undocumented fan-seize | FIXED@6a7efcf — thermal scene drives INLET across its 42°C warning threshold + CPU1 rise (doc 09 §Phase 6); the fix required the R7-P1 doc-13 §5.2 amendment: a >5σ jump TOWARD the degradation direction keeps the baseline (alerting) instead of silently re-learning — direction comes from the skill's trending rule; cross-subsystem scene emits once with a REAL Pearson r computed from the two sensors' observed series (waits for enough aligned samples, no fabricated coefficient); summary is the doc-09 §Phase 7 dashboard (verdicts/peer-witness+last-known/correlation/actions/capabilities — ✓ only when actually demonstrated); `--scenario` with all 7 modes (fan-seize CRITICAL moved into fan-failure only — it masked the PSU diagnostics action in `all`); PSU-Absent + thermal-warning rules now propose their doc-09 actions (all 3 pending at summary); sigma messages rounded; all 6 single scenarios verified live + 4 new e2e assertions |
| QA-029 | Console UI | MAJOR | SPA splits /api across two backends (CC 8090 + Console 8100) with no reverse proxy — half the screens 404 in every shipped config; L3/L4 boundary blurred | FIXED@5c21a0d — Console proxies 10 CC prefixes w/ bearer forwarding (verified live: /api/fleet/ + /api/fleet/summary through Console origin); two live sub-bugs fixed: PEP-563 string annotation made `request` a query param; collection-root 307 loop between origins |
| QA-030 | CI | MAJOR | CI never builds containers or UI; coverage gate not enforced; no compose-boot gate | FIXED@ac92100 — CI: ui-build job (both UIs) + compose-gate job running scripts/e2e-compose-gate.sh (the amendment-§8 scenario against the real booted stack); gate validated locally |
| QA-031 | Agent | MINOR | Playbook executions in-memory only (crash loses resume state) | FIXED@62577ac — `playbook_executions` checkpoint table; PlaybookExecutor persists after every step + terminal state; on restart RUNNING executions come back PAUSED w/ honest "interrupted; step outcome unknown" (persisted flip, mirrors load_actions EXECUTING handling); agent loads them at startup (`Agent.playbook_executions`) and warns; 5 tests |
| QA-032 | Agent | MINOR | ECC counters baselined as raw values not rates (doc 13 §5.6) | FIXED@fd44675 — `counter: true` on skill trending rules (loader-validated); TrendingEngine.counter_to_rate converts to delta/hour, detects+logs resets (skip, re-prime), zero-rate stays HEALTHY; compute_trend uses the RATE as current value; memory-health.yaml marks ecc_correctable_lifetime as counter w/ `{current_rate}/hr` message; 6 tests |
| QA-033 | SM/CC | MAJOR | Knowledge distribution: proto field `learned_patterns_json` doesn't exist; distributor/feedback outside every loop; SM PushPolicy is log-and-ack stub | PARTIAL@pending — DISTRIBUTION half closed: `PolicyUpdate.learned_patterns_json = 5` added + proto regenerated (scripts/gen_proto.sh, toolchain-matched); `distribute_patterns()` runs in the intelligence loop each cycle (scope-matched sites via KnowledgeDistributor.select_targets, per-(pattern,site) dedup, failed pushes retry); SM PushPolicy upserts into new `sm_fleet_patterns` (migration 0004) + in-memory mirror; matching patterns now ride into LLM enrichment as `fleet_pattern` evidence (loaded from DB at SM startup); 10 tests. OPEN half: LearningFeedbackTracker stays library-only — it needs the SM→CC candidate-skill upflow that doesn't exist (SM SkillGenerator/validate_and_promote have no callers); belongs with the §5 ledger-corrections item, needs its own slice |
| QA-034 | Agent | MAJOR | Credential rotation stub (`return True` x4); credential validation (R-CV1-5) absent | FIXED@pending (decided: Vinod, A10.5 — implement, not de-claim) — all four Redfish AccountService calls real: create via POST (405 fixed-slot fallback for older iDRAC), verify by opening a FRESH session as the new account, disable via PATCH Enabled:false, rollback delete (best-effort, DELETE w/ slot-clear fallback); RedfishClient gains generic delete() + 204/empty-body tolerance; simulator gains a full AccountService (multi-account session auth, password write-only) so verification is genuine; 4 live integration tests (success, verify-fail rollback, dupe username, password never echoed) |

| QA-036 | Console UI/CC | MAJOR | SPA called /api/groups + /api/autonomy (bare) which exist nowhere; CC lacked group detail + member add/remove routes the UI always called | FIXED@5c21a0d — 13 SPA call sites rewritten to /api/policies/...; CC gained GET /groups/{id}, POST/DELETE members (audited, 3 tests) |
| QA-037 | SM/CC | BLOCKER | RegisterSite chicken-and-egg: SM token interceptor required the site token on the very RPC that ISSUES it — CC could never register a site; also CC 409'd forever on half-registrations; found LIVE (fleet poll UNAUTHENTICATED) | FIXED@8b22311 — interceptor exempts /harkeniq.v1.SiteManagerService/RegisterSite (fingerprint is the bootstrap credential, HARKEN_SM_LICENSE_FINGERPRINT enforced when set); CC heals token-less sites; 3 over-the-wire tests (direct-call suites could never catch an interceptor bug) |
| QA-038 | Tests | MINOR | test_directives bound the real 50051 (unrunnable beside a dev stack) | FIXED@8b22311 — grpc_port=0 |
| QA-035 | Console | MAJOR | `/api/internal` endpoints have NO auth (docstring: "No auth (internal network)") despite R5-2 ledger claiming a CC->Console credential pair | FIXED@47ce04a — /api/internal enforces the CC<->Console bearer key (constant-time; secure+unconfigured fails closed 503; 5 tests); compose wires matching keys |

| QA-039 | Demo | MAJOR | Fresh stack proposes NO actions for 15 min (min_samples=60 x 15s polls — A2.3 gate working as designed, wrong demo default); found live by the compose gate | FIXED@ac92100 — demo stack sets HARKENIQ_BASELINE_MIN_SAMPLES=5 (~75s to confidence 1.0); DEMO.md reset section documents the wait as an A2.3 talking point |

| QA-040 | SM | BLOCKER | RegisterAgent crashes on postgres: `agent_identities.public_key_pem`/`certificate` mapped bytes onto Text (asyncpg DataError; sqlite tests accepted bytes silently) — NO agent identity/certificate ever persisted on the real stack, so the whole QA-020 lease chain was dead in compose while the gate read GREEN (observe→approve works without identity). Found live 2026-08-26 by an ERROR-log sweep after a gate run | FIXED@919236e — columns → LargeBinary (certificate is canonical JSON + raw Ed25519 sig, not UTF-8); SM migration 0005 (BYTEA alter, idempotent, proven both directions on live postgres); verify_agent_signature str-guard for legacy sqlite rows; postgres-gated regression test (register→verify→peer-bundle roundtrip); gate hardened: asserts persisted identity+certificate AND no ERROR/CRITICAL logs in any service; gate3 rerun GREEN from scratch with agent "SM identity bootstrapped" |
| QA-041 | Agent | MAJOR | Agent registration is fire-once: startup RegisterAgent failure logs "continuing standalone" and never retries (next registration only on firmware-inventory change) — a transient SM/DB hiccup at boot leaves the agent lease-less until restart | FIXED@pending (decided: Vinod — piggyback on the report loop, no new loop) — registration extracted to `Agent._register_with_sm()`; `_report_loop` retries each cycle until `_sm_registered`, paced by the reporter's existing backoff; stops cleanly once registered; 3 integration tests (flag only on success, loop recovers after failure, no re-registration spam after success) |

(Statuses updated in place as the campaign proceeds; new live findings appended.)

## Test log

| When | Area | Result |
|---|---|---|
| 2026-08-25 | Baseline | Full suite 2157 collected / 2155 pass, 2 expected skips; e2e 17/17; `harken demo` exit 0 |
| 2026-08-25 | Boot+Auth | FIRST fully healthy fresh boot: 7/7 services healthy; agent registered+OBSERVING at SM; CC+Console in SECURE mode accept a real Keycloak token (password grant, platform admin), reject absent/garbage tokens with 401 |
| 2026-08-25 | Demo flow | FULL RUNBOOK EXECUTED LIVE: seed (tenant+site, token healed) -> CC fleet shows agent w/ device_class -> Console proxy serves CC data -> fault injected -> agent CRITICAL verdict -> SM incident (confidence 1.0) -> COLLECT_DIAGNOSTICS proposed -> approved via API |
| 2026-08-25 | Compose gate | GATE GREEN from scratch (gate3): build -> boot -> healthy -> seed -> register -> observe -> CC fleet -> Console proxy -> 401 negatives -> fault -> incident -> proposal -> named approval (sm-local:ci-gate) -> audit chain valid. Wired as a CI job |
| 2026-08-25 | Autonomy chain | QA-020/021/022 wired: full suite 2237 passed / 2 skipped (from 2155 baseline; +~80 tests). SM leases verified carrying real budgets+stop_switch+suppression over direct servicer calls; CC stop-switch flip verified pushing to 2 fake SM sites; agent gate chain verified refusing on stop switch/preconditions/blast radius and executing clean approved actions |
| 2026-08-26 | Compose gate | Post-campaign gate re-run from clean slate (`down -v`, full `--build` at `6a7efcf`+): scenario GREEN, but an ERROR-log sweep caught QA-040 (RegisterAgent postgres crash — identity chain dead while gate read GREEN) and QA-041 (registration never retried). QA-040 fixed (BYTEA + migration 0005, proven both directions on live postgres; postgres-gated regression test); gate hardened with identity-persisted + no-ERROR-logs steps; final clean-slate rerun GATE GREEN (14 steps), agent logs "SM identity bootstrapped", full suite 2285 passed / 3 skipped |
