"""Device protocol abstractions (R4-0 Phase 5).

The DeviceProtocol interface decouples the agent's polling, reasoning,
and action layers from any specific BMC protocol. Current implementation:
RedfishProtocol. R4-1 adds IPMIProtocol.
"""
