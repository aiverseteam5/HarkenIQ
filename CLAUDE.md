# HarkenIQ — Project Constitution

HarkenIQ is the vendor-neutral AI SRE for data center hardware: an embedded brain for
every server regardless of vendor ("they bolt on, we build in"). Hardware becomes an
agentic, intelligent device through three earned tiers — **Observe** (read-only
diagnosis) → **Approve** (human-approved actions in seconds) → **Autonomy** (proven
actions run unattended within budgets, with a stop switch).

## Governing spec — read this first

**`docs/requirements/00-platform-spec.md` (dated 2026-08-19) governs all engineering.**

- Detailed requirements: `docs/requirements/01–13` (R-M*/R-S*/R-C*/R-X* IDs authoritative).
- Product vision/personas/pricing: `HarkenIQ_PRD.md` + `HarkenIQ-Platform-Design.md`.
- **NOT build order:** doc 02 §8 market phasing, PRD 30/60/90 plan, Platform-Design
  5-phase plan, and TODOS.md's "release one is Harken Mesh" — all superseded per spec §0.
- **Change control:** scope changes only by dated amendment to spec §9. A slice may
  narrow itself; it may never redefine another slice or rename a layer. Do not
  re-litigate scope at phase boundaries.

## Fixed vocabulary — four layers (spec §1)

| Layer | Name | One line |
|---|---|---|
| L1 | **Harken Node (Mesh)** | Per-device agent; detects all single-device faults |
| L2 | **Site Manager** | Per-site multi-device correlation, incident consolidation, command brokering |
| L3 | **Central Command** | Per-tenant fleet intelligence, learning, authorization, approvals (= old "Cluster Manager", name retired) |
| L4 | **Harken Console** | Vendor multi-tenant SaaS: tenants, onboarding, RBAC, billing, support, super admin |

Correlation boundary: single-device fault → node; multi-device comparison → Site Manager
(spec §1, doc 01 §4.3.1).

## Status ledger (update at every milestone commit)

| Date | Milestone |
|---|---|
| 2026-08-19 | **R1 Diagnostic Foundation shipped** — commits `16cfca8` (Phase 2) + `5128fbd` (Phase 3) on origin/main; 498 tests; Dell/HPE Redfish, skills, baselines/trending, state machine, checkpoint, peer heartbeat/witness, action queue + CLI approval, TUI, gRPC reporter stub, `harken demo` |
| 2026-08-19 | Master spec `00-platform-spec.md` + this constitution adopted |
| 2026-08-19 | **R2a Site Manager shipped** — `harkeniq_sm` service (gRPC ingest TLS+token, TimescaleDB-ready schema, site model + domain inference, correlation engine with 4 rules, incident consolidation, approval brokering, FastAPI + React dashboard v1, Docker Compose); agent: registration/action-sync/decision-poll RPCs, secure channel; CLI `peers list` + `bmc test`; frozen exit gate green; 631 tests, 87% coverage |
| 2026-08-22 | **R2b Central Command + Console v1 shipped** — 7 phases (`1e4b097`..`82ea9f9`); CC service (fleet poller, usage reporter, approval routing, autonomy budgets); Console service (Keycloak OIDC, tenant/user/RBAC, Ed25519 licensing, billing engine with commit+overage invoicing, Razorpay+Stripe adapters, delinquency state machine, metering+air-gapped upload, support ticketing+24h mode, audit logs+export, feature toggles, release mgmt, API keys, impersonation log); 22 production React screens; 25 Console DB tables; 1188 tests, exit gate green |
| 2026-08-23 | **R3 re-sliced** — Amendment A2 to spec §9; R3 split into R3a/R3b/R4; 6 gating OQs answered; 7 architectural contracts defined |
| 2026-08-23 | **R3a Safe Autonomy + Outcome Loop shipped** — 4 commits (`a969d3f`..P8); agent Ed25519 identity + SM-signed leases + tier gating; risk-degraded partition behavior; resource profiles (constrained/standard/performance); 4 new actions (SEL clear, BMC reset, power cycle, power cap adjust) with preconditions + blast radius + verification; autonomy budgets (distributed CC/SM/agent) + stop switch; outcome tracking + error-budget drop-back; correlated-conclusion suppression (2 trigger paths + auto-recovery); OS signals (syslog/dmesg) + hardware-to-OS device mapper; Diagnosis model; `harken diagnose` CLI; 7 A2.7 contracts verified; 1331 tests, exit gate green |
| 2026-08-23 | **R3b-1 Intelligence + Skills shipped** — 3 commits (`943ceef`..R3b-1); LLM Explain at SM (ReasoningPipeline + LLMReasoner, backend-agnostic httpx provider); candidate skill generation; OS signal expansion (journal + smartctl); full hardware-to-application mapping (drive->block->mount->process->service); SkillPackage<->loader integration; skill validation pipeline (static analysis + dry-run); skill distribution (PushSkill RPC + agent SkillReceiver); KB persistence tables; 1397 tests, exit gate green |
| 2026-08-24 | **R3b-2 Full Mesh Protocol shipped** — 8 phases; peer key distribution (SM-brokered PeerKeyRing + signed bundles); message type envelope (4-type UDP multiplexing); claim data model (Ed25519-signed, canonical JSON wire format); claim broadcast + ack (UDP reliability layer with retry); first-claim-wins ownership (deterministic tiebreak on agent_id, not timestamp); claim lease management (120s lease, lapse inheritance per R-M17); quorum disambiguation (4-way: DEVICE_DOWN/LINK_DOWN/NODE_FAILED/ISOLATED); suspicion exchange (per-component scores, threshold-triggered claims, greedy set-cover R-M21, bundle coverage R-M22); two-device correlation probe (OQ-13: LOCAL_PORT/REMOTE_PORT/CABLE/INCONCLUSIVE); partition fencing (isolation detection, fenced mode, recovery); all 4 PeerProtocol stubs implemented (Contract 7 complete); OQ-4 + OQ-13 answered; Amendment A3; 1523+ tests, exit gate green |
| 2026-08-24 | **R3b-3 Advanced Remediation + Fleet Learning shipped** — 8 phases; CredentialProvider interface (Local+Vault+Mock+Chain, Vault via httpx KV v2, R-H7 fallback); credential rotation (blue-green: create→verify→disable, rollback on failure); multi-step playbooks (Playbook/PlaybookStep/PlaybookExecution, PlaybookExecutor with per-step verification+rollback+resume, 3 built-in playbooks); fleet outcome reporting (FleetOutcome proto in FleetSnapshot, SM watermark, CC cc_outcome_history); outcome aggregation (by action_type/vendor/model, success/failure/resolution rates, trend detection); fleet pattern detection (batch_failure/anomaly/reliability, cc_fleet_patterns table); knowledge distribution (CC→SM routing by scope match, PushPolicy channel); learning feedback loop (R-C1 complete: outcome→pattern→skill→distribute→outcome, auto-promote at 95% success); OQ-14 answered; Amendment A4; 1660+ tests, exit gate green |
| 2026-08-24 | **R4 Architecture Amendment approved** — R4 split into R4-0/R4-1/R4-2/R4-3; reference architecture diagram; DeviceProtocol abstraction; IPMI as first non-Redfish proof point (R4-1); air-gapped LLM workload analysis; OQ-16 partial (IPMI), OQ-18 deferred; design doc at docs/designs/r4-architecture-amendment.md |
| 2026-08-24 | **R4-0 Platform Validation shipped** — 7 phases; unified Docker Compose full-stack (Agent+SM+CC+Console+TimescaleDB+Keycloak+MockSimulator); Alembic migration chains for CC+Console (backfilled 0001_initial.py); structured JSON logging with request-id propagation (JSONFormatter + middleware); health checks (HealthChecker with pluggable probes) + Prometheus metrics (MetricsRegistry with text export); DeviceProtocol abstraction (interface + RedfishProtocol wrapper, 63% of codebase verified protocol-agnostic); E2E exit gate; Amendment A5; 1730+ tests, exit gate green |
| 2026-08-24 | **R4-1 Infrastructure Breadth shipped** — IPMIProtocol (pyghmi IPMI-over-LAN RMCP+ UDP 623, no root; FRU identity, sensor→NormalizedDevice normalization, SEL→log entries, IDENTIFY_LED + atomic SEL_CLEAR); agent wired through DeviceProtocol factory (`bmc.protocol: redfish\|ipmi`, Redfish default; RedfishDeviceProtocol wrapper fixed + made real); ActionExecutor protocol dispatch (allow-list/audit stay agent-side, vendor coupling Redfish-only); MockIPMIBMC in-process test double with fault injection; cross-site correlation R-C2 (site-aware OutcomeAggregator, cross_site_batch pattern ≥2 failing sites, IntelligenceEngine loop wired into CC runtime, OutcomeHistoryRepo+FleetPatternRepo, /api/outcomes/metrics + /api/outcomes/patterns); Console Vendor Reliability page (/reliability); LLM local endpoint validated (llama.cpp /v1 URLs, key-optional auth, opt-in live probe); OQ-16 answered (server BMC extensibility proven); exit gate green |
| 2026-08-24 | **R4-2 Fleet Intelligence shipped** — compliance audit hash chain across all 4 stores (shared `harkeniq/audit/chain.py`, SHA-256 + per-service seq, chained at write, verified on demand via repo methods + `/api/audit/verify` endpoints + agent `verify_audit_chain()`; OQ-20 answered); config drift detection + remediation (ConfigPolicy YAML + drift detector, `collect_config()` on DeviceProtocol, CONFIG_RESTORE action with Dell DellAttributes restore+verify, PlaybookExecutor FINISHED — `_run_action` stub wired to real ActionExecutor with allow-list/audit intact, rollback real, dry-run default per risk register, agent compliance loop + approval-gated playbook execution); firmware inventory (R-AGENT-17/18: cross-vendor version comparator, protocol `collect_firmware_inventory()`, `inventory_interval` loop brought alive, proto tags AgentRegistration.firmware_json + FleetDevice.service_tag/inventory_json, SM Device.firmware, CC fleet cache) + local CVE feed matching (`cc_cve_feed`, import/exposure API, air-gap safe, example bundle in deploy/); warranty/lifecycle (Dell TechDirect adapter per documented OAuth2+asset-entitlements v5, HPE has NO public server warranty API — manual import endpoint instead, `cc_warranty` TTL cache + refresh loop, warranty on fleet list + new `/api/fleet/{id}` detail endpoint fixing pre-existing Console 404, FleetOverview warranty+firmware panels); Amendment A6; 1828 -> 1933 tests, exit gate green |
| 2026-08-24 | **R4-3 Governance & Ecosystem shipped** — community skill marketplace at Console (OQ-22 answered: community→verified→core tiers, `SkillTier.VERIFIED`, submission w/ `parse_skill` schema validation as the untrusted-YAML safety boundary, human review queue, install + stats, promotion gate on RAW counts ≥95%/≥50 exec/≥50 devices, `skill.submit/review/install` permissions, audited on the R4-2 chain, Console UI page); air-gapped LLM serving (OQ-18 closed: llama.cpp compose service under `airgap-llm` profile, SM model-integrity gate `llm_model_path`+`llm_model_sha256` — corrupt/missing model disables LLM, R4-0 HealthChecker finally wired into SM `/healthz` w/ db+model probes and model metadata); firmware update orchestration (OQ-21 answered: SM `firmware_campaigns`+targets, blast-radius wave planner — never 2 devices of one fault domain in a wave, campaign-level human approval, strictly sequential waves, halt-on-first-failure with blue-green standby-bank rollback, DeviceUpdater seam that refuses to advance without a real transport; agent FIRMWARE_UPDATE/FIRMWARE_ROLLBACK actions — risk "high", health-gated preconditions, Redfish SimpleUpdate + task poll + version verify; simulator UpdateService w/ task state machine + failure injection); predictive maintenance infrastructure (CC `predictive.py`: recency-weighted device failure rate, cohort prior, health/warranty modifiers, explicit insufficient_data, `/api/predictive/risk` — deterministic scoring, ML seat reserved pending data accumulation per §3); Amendment A7; 1933 -> 2011 tests, exit gate green |
| 2026-08-24 | **R5-1 Directed-Directive Transport shipped** — the SM→agent transport both R4-3 seams waited on: `sm_directives` table + DirectiveService (queue → deliver-on-agent-poll → settle; audit-chained), new `PollDirectives`/`ReportDirectiveResult` RPCs, agent-side execution in background tasks through its OWN executor (allow list + preconditions + audit apply — delivery is not a policy bypass; kind=action incl. FIRMWARE_UPDATE via playbook-grade path, kind=skill_install via SkillReceiver + hot reload); `AgentDirectedUpdater` implements the firmware DeviceUpdater over directives and is wired into SM runtime — **campaign advance now drives real agents** (proven end-to-end: orchestrator → gRPC → agent → simulator BMC flash → settle → completed); SM `POST /api/skills/install` queues marketplace skills to agents (static-validated before queueing). Also: **console-ui build FIXED** (25 tsc errors → 0; PageHeader accepts action arrays, ConfirmDialog accepts children, FilterBar `date` type, dead imports removed; `npm run build` green for the first time since R2b). 2011 -> 2031 tests |
| 2026-08-24 | **R5-2 Completion + Enterprise Hardening shipped** (Amendment A8, scope approved by Vinod) — marketplace install automation Console→CC→SM→Agent (CC PULLS installs from a Console internal endpoint using the existing CC→Console credential pair — no new trust direction; `marketplace_installs` events at Console, `InstallSkill` RPC CC→SM queueing skill_install directives, `cc_skill_deliveries` durable dedup ledger with failed-push retry, sync loop in CC runtime; full chain proven over real ASGI Console + real SM gRPC); audit-chain multi-replica locking (`pg_advisory_chain_lock`: transaction-scoped PostgreSQL advisory lock held from tail-read through commit, wired into all three service appends, no-op on sqlite — removes the documented GA limitation); CC tenant scoping for `cc_fleet_patterns`/`cc_cve_feed`/`cc_warranty` (tenant columns, warranty composite PK (tenant, tag), all repos/APIs/loops threaded — same service tag can carry per-tenant records without leakage). **Deferred by A8 (not abandoned):** Network Intelligence milestone (OQ-16 remainder; gNMI-vs-NETCONF decision deferred WITH it), trained predictive models (data gate). 2031 -> 2049 tests, exit gate green |
| 2026-08-25 | **R6 Network Intelligence scoped — Amendment A9 approved** (decided: Vinod) — anchor: community SONiC container; protocols: gNMI primary (streaming per R-M3) + NETCONF config-ops, both behind DeviceProtocol, NETCONF simulator-gated until a real NETCONF device is available; placement: N0 on-switch from day one (constrained profile); actions: LED locate + counter clear (low) and interface reset/disable (high, T1 quorum + SM + CC approval, redundant-path preconditions, self-preservation invariant). Design doc `docs/designs/network-intelligence-milestone.md`; A9 in spec §9 (with A5–A8 pointer row) |
| 2026-08-25 | **R6 Network Intelligence shipped** — 8 commits (`f434f3d`..P8); P0 reality spike (agent 43.3MB containerized vs 50MB constrained cap → switches run STANDARD profile per D12; real-SONiC gNMI capture = P2 fixture: gnmi server is a separate container sharing netns+redis, OID-keyed COUNTERS_DB via name map, SAMPLE-only subscribe at 1s flexcounter floor, translib-write disabled by default, Set accepted≠persisted → read-back verification mandatory; **P5 NETCONF dropped by D13** — absent on community SONiC, defers to partner-site gate); P1 model (`protocols/model.py` move + shim, rate-typed NormalizedInterface, device_class, fault-domain hierarchy); P2 switch simulator (P0-fixture-faithful gNMI server + 6 fault injections); P3 GNMIProtocol (subscribe cache, staleness→TimeoutError, counter→rate layer w/ wrap+reset suppression, wall-clock O(1) window features, TOFU TLS pinning w/ ALPN+CN override, read-back-verified actions; 64-port load test); P4 interface-health skill + R-M5 hardware-vs-congestion classification + probe on real counters; P6 action safety chain (INTERFACE_DISABLE/RESET/ENABLE/CLEAR_COUNTERS; self-preservation route-resolver fail-closed, T1 quorum gate, LAG blast radius never-2-of-one-LAG, enable restore-semantics); P7 device_class through proto/SM/CC/Console (+0002 migrations) + tor_connectivity correlation rule (A2.6 Connectivity live); P8 N0 packaging (Dockerfile.agent, compose `network-sim` profile, GHCR multi-arch publish workflow w/ smoke gate) + **exit gate GREEN incl. LIVE real-SONiC streaming (32 ports) and enforced-cap container run**; fidelity finding: real server resolves DB target from request PREFIX. Partner-site gates open by design (§5). 2049 -> 2155 tests |
| 2026-08-26 | **R7 Demo Hardening shipped — Amendment A10** (decided: Vinod) — campaign register `docs/qa/r7-bug-register.md` (QA-001..041): boot truth (alembic entrypoints, real healthchecks, agent in default stack), real Keycloak auth everywhere (JWKS, 401 negatives), demo truth (narrative clock, DIAGNOSIS panel, doc-09 scenes in 7 scenario modes), autonomy chain wired end-to-end (QA-020/021/022: `_execute_gated` preconditions→lease→blast-radius on both paths; SM `AgentIdentityService` constructed w/ persisted keypair; CC stop switch persisted+pushed), OS signals + BMC log poll live, playbook resume, compose exit gate `scripts/e2e-compose-gate.sh` (boots real stack, drives ten-step scenario, asserts persisted identity chain + zero ERROR logs; wired as CI job). Late finds: QA-040 (`919236e` — agent_identities bytes-into-Text killed RegisterAgent on postgres; BYTEA + migration 0005) and QA-041 (fire-once registration; now retried by report loop). QA-034 rotation made real per A10.5 (Redfish AccountService calls + simulator AccountService + live tests). Ratified: budget→action-class mapping (A10.4), approval-vs-lease semantics (A10.3). OQ-16 CLOSED (A10.2, NETCONF dropped per D13). 2155 -> 2290+ tests, gate GREEN |
| 2026-08-26 | **R7 register CLOSED — R-C1 learning loop complete** (QA-033 feedback half + QA-042): SM ingest generates candidate skills from LLM diagnoses (SkillGenerator + validate_and_promote finally have callers; `sm_candidate_skills`, migration 0006); candidates ride new `FleetSnapshot.candidate_skills` proto → CC `cc_candidate_skills` (migration 0004, tenant-scoped); IntelligenceEngine runs LearningFeedbackTracker: pattern→cycle→candidate link→distribution reach→measured improvement→promotion recommendation at 95%/50 devices (marketplace human review still gates distribution); `/api/learning/*`. QA-042: `SMClient.get_fleet_snapshot` never dictified outcomes — CC fleet learning ingested NOTHING since R3b-3; fixed + over-the-wire regression test. Every register row FIXED. 2301 tests, gate GREEN |
| 2026-08-29 | **P1 S5 Autonomy shipped** — the governed decision boundary for action, on `feat/p1-agentic-product`. `GET /api/autonomy` (fleet.view per D2) is a stable machine-readable governance contract, not a page DTO: per-class disposition + reason + `blocking_conditions` (tenant/site/domain-scoped) + outcome evidence + learned signals + live safety + approval requirement + advancement distance, for all 14 action types. The A10.4 ladder was promoted out of `policy_push` into `harkeniq_cc/autonomy.py` and is now ONE object both the contract and the CC→SM push consume (test fails if they diverge); "no risk-`high` action is ever budget-grantable" is a derived invariant over `ACTION_RISK`, not a hand-kept list. New additive proto `FleetSafetyState` (suppressions + error budgets + site budgets) → `cc_safety_state` (CC migration **0007**) carries safety out of the Site Manager for the first time; unreported reads UNKNOWN, never safe. **Defect found and fixed by the slice:** R3a's ratified A2.2 error-budget drop-back had NO runtime writer — `KnowledgeBase` was never constructed, `sm_error_budgets` never written, `is_action_type_dropped_back` never called — so a class could fail repeatedly and keep its autonomy; now folded at `ApprovalService._record_outcome` and enforced in the lease as `propose` (not `deny`). Console Autonomy page + proxy prefix + nav. Proven on the live stack: real agent action → SM outcome → error budget → snapshot → CC → contract; level 2 grants SEL_CLEAR/BMC_RESET, stop switch denies everything, FIRMWARE_*/INTERFACE_RESET/DISABLE denied at every level. Sequencing (Vinod): the 9 unmapped action classes and tenant-plane drop-back recovery are recorded as capability-registry candidates (design doc §11), not silent drops. 2524 → 2592 tests |
| 2026-08-30 | **P1 A0+A1 Operational Agent shipped** — the product noun becomes an object, on `feat/p1-agentic-product`. A declarative bundle over capabilities that already exist: identity + version (attribution key `op-agent:<id>@v<n>`), explicit scope rows (no rows = no devices), capability BINDINGS that only reference what exists, and a policy that can only ever tighten the tenant's own. `cc_operational_agents`/`cc_agent_scopes`/`cc_agent_capabilities`/`cc_agent_proposals` + `cc_outcome_history.actor` (CC migration **0008**); SM `sm_directives.actor/authorization_basis/proposal_id` + `sm_action_outcomes.actor` (SM migration **0007**). **CC-resident evaluator** (decided: Vinod) reads the same `build_attention` + `build_autonomy` composers the Console reads — one `governance.py` loader now serves both, so the operator and the agent can never see different governance. Proposals land in the **ONE** `/api/approvals` queue under the same `action.approve`; one new additive RPC `DispatchAction` (CC→SM, same channel + site token as RouteApproval) queues on the R5-1 directive transport, and the node funnel is untouched. New load-bearing rule: a directive's `authorization_basis` decides whether an authorization-shaped lease verdict is already satisfied — `autonomous_grant` may NOT proceed past `propose`, which is what finally lets the S5 error-budget drop-back stop an agent. **Defect found and fixed:** directed actions produced NO outcome record at all (`_run_directed_action` builds its Action outside the reported queue; `report_result` wrote only audit) — every firmware-campaign execution since R5-1 was invisible to the error budget and to fleet learning; one shared writer (`harkeniq_sm/outcomes.py`) now serves both paths. Model corrections the slice made to itself: `not_budget_mapped` means "always needs a human", not "forbidden" (this closes the §11 approval half for the 9 unmapped classes without widening any autonomy boundary), and a tenant stop switch denies every class. Also fixed: the Approvals page decided by the wrong id, sent the wrong batch contract, and rendered five fields CC never sent. Console Operational Agents page + proxy prefix + nav. Proven on the live stack BOTH ways: human-approved COLLECT_DIAGNOSTICS ran and came back SUCCESS attributed to the agent; autonomous SEL_CLEAR dispatched on the contract's grant and was **refused by the node's own allow list**, and that failure became attributed evidence in the error budget. 2592 → 2683 tests |
| 2026-08-30 | **E0.1 approval enforcement shipped** — first slice of the ratified Enterprise Governance Foundation (E0+E1+Registry), spec amendment **A15**. `cc_approval_policies` carried `required_approvers`, `approval_mode` and a group link since R2b and NOTHING consulted them at decision time: a tenant could configure dual authorization and get single authorization (4th declared-with-no-caller instance). A decision is now a SET: `cc_approval_records` (CC migration **0009**), one row per approver, `unique(subject_type, subject_ref, approver_ref)` making duplicate prevention a DB guarantee; `cc_approval_routes.decision` becomes a projection. Judgement is pure (`approval_policy.py`: most-specific policy resolution, required count, group membership by Keycloak sub with email fallback, completion where a denial is terminal); enforcement is in the ONE decision function both origins already called, so node actions and agent proposals cannot diverge. **Three further defects found:** (a) `approval_mode: auto_approve` would have become a second ungoverned path to unattended execution bypassing evidence, budget, drop-back and the high-risk fence — refused on write, coerced on read (A15.7), UI presets retired; (b) approval policies were **uncreatable on PostgreSQL** — `created_by` was String(32) vs a 36-char Keycloak UUID, passing only on sqlite (2nd instance after QA-040; now guarded by a model-invariant test over identity column widths); (c) `risk_level` defaulted to "medium" while the other selectors defaulted to "*", so "dual approval for everything" governed medium risk only. Proven live with two real Keycloak identities: 1 of 2 records without executing, duplicate → 409, second approver dispatches a real directive, ledger names both, denial terminal, one audit entry per approver, chain valid. 2683 → 2737 tests |
| 2026-08-30 | **E0.2 authoritative site identity shipped** — spec amendment **A16**; hard prerequisite for E1.3. The review reported one "return all devices" fallback; tracing the runtime found **six** leak paths, two of them destructive: devices (fallback), incidents (no filter), pending actions (no filter), outcomes and candidate skills (no filter **and** the `reported_to_cc` watermark, so one site's poll CONSUMED another's evidence and that site never received it), and `GetUsageSnapshot` counting the whole SM but labelled with one site — every site on a multi-site SM billed for the entire fleet. `sites.cc_site_id` (unique) is now persisted at RegisterSite and is the only resolution path: idempotent re-registration, rename allowed, second site created alongside, **conflicting re-point refused and audited**, recovery via an audited break-glass unbind (typed site-name confirmation + reason) at the site-token API. Unresolved → empty snapshot with `site_resolved=false` + reason (additive proto); CC's poller **skips ingest** on it, because ingesting would clear the fleet cache and D3-resolve every incident, and re-registers instead — which is also how existing deployments cross the upgrade with no SM-side adoption guessing. **Error budgets are now per site** (`sm_error_budgets` keyed `(site_id, action_type)`): withdrawal, recovery, the dispatch gate and the agent's lease all resolve from the device's own site, and the migration carries existing drop-backs onto the site rather than losing a withdrawal. SM migration **0008**. Proven on real PostgreSQL with two sites on one SM: upgrade self-healed, symmetric isolation for devices/incidents/actions/outcomes/budgets, polling A left B's outcome unreported and vice versa, each site metered 1 node not 2, conflicting registration refused, `site.bound`/`site.unbound` audited on a chain that still verifies. 2737 → 2772 tests |
| 2026-08-30 | **E0.3 shipped — E0 COMPLETE** (no new amendment: implements A13, applies D2's existing read-split consistently). **`/metrics`**: `MetricsRegistry` shipped with R4-0 and had ZERO callers — all three services could say they were alive and nothing about what they did. Now mounted on CC/SM/Console with a request-counting middleware (mounting a permanently-zero endpoint would be the same declared-with-no-writer pattern), registry per-app not module-global so two services in one process cannot report each other's traffic, unauthenticated like `/healthz` with no tenant identifiers. **A13 read gates**: role-bundle listing `role.manage`→`user.view` (a read gated on a mutation permission); approvals list/history/records `action.approve`→`action.approve` OR `audit.view` via a new read-only `require_any_permission`; policy + group reads `site.manage`→`fleet.view`, matching S1's autonomy-budget precedent. No permission invented; a viewer still cannot read approval evidence; every mutation asserted to have stayed put. **Skill binding**: A0 accepted `kind:"skill"`, validated nothing, wired to nothing — refused now with a reason naming A2, which owns the four missing pieces (Console skill-by-id endpoint, CC fetch, per-device `InstallSkill` targeting, activation trigger). Live: `/metrics` counters move on all three; a real Keycloak auditor reads approvals/history/policies/groups/audit/fleet/incidents/autonomy (all 200) and is refused every mutation; skill binding refused. **Honest limit:** the Console role-bundle gate is test-proven but not live-exercisable — the Console honours tenant roles only from a tenant realm and the demo runs one platform realm (TODOS "SPA realm discovery", owned by E1.4). 2772 → 2808 tests |
| 2026-08-30 | **E1.1 organizational tree shipped** — the first Enterprise Governance Foundation slice, and deliberately the one with NO authorization effect: `cc_org_units` (generic tree, customer-named `unit_type`, never an enum) + `cc_sites.org_unit_id`, CC migration **0010** guarded and idempotent with a backfill giving every tenant one root and every existing site a canonical path. Materialized `/id/id/` path with a **trailing delimiter** so a subtree is one prefix match that behaves identically on PostgreSQL and sqlite — chosen over a recursive CTE because E1.2's scope resolver stands on this query, where a divergence would be an authorization bug not a display bug. Server-enforced: depth bounded at 8 (checked over the whole moving subtree, not just the dragged node), cycles refused, sibling names unique per (tenant,parent), delete refused while a unit holds children or sites, a move rewriting every descendant path in one audited transaction. `/api/org-units/*` + `PUT /api/sites/{id}/org-unit` at existing `site.view`/`site.manage` — no new permission. Console Organization page + proxy prefix + nav. **Containment is not authorization** (decision B) is asserted, not merely documented: the compose gate proves moving a site between units leaves `/api/autonomy` byte identical and the fleet read unchanged. Cross-engine prefix equivalence proved on real PostgreSQL vs sqlite, including a deliberately variable-width id and a poisoned `/%/` path. Live: 4-level tree, re-parent rewriting a branch, cycle 400, depth-9 400, delete-with-contents 409, auditor 200/403, operator 403, chain verifies. 2808 → 2896 tests |
| 2026-08-30 | **E1.2 scoped RBAC shipped** — server-side authorization, one resolver for humans and agents. `cc_scope_grants` (five scope types: tenant/org_unit/site/device_class/device — the last two survive the merge or every A0 agent loses reach) + `cc_tenant_settings` + `cc_approval_records.scope_snapshot/authority_snapshot` (L2) + `cc_audit_log.site_id`; **`cc_agent_scopes` migrated in and DROPPED**; CC migration **0011**, `legacy_open` seeded so behaviour is unchanged on upgrade. Two questions, two places: the route guard asks *could this actor ever* (all 68 sites unchanged), `ResolvedScope.permits(permission, target)` asks *here* — they cannot be one question because `permission_subset` is PER GRANT, which is what vindicates "no scope in the decorators". Enforced at four layers: guard → repository read filter (10 site-anchored tables, counts scoped too) → object gate (26 mutations) → approval gate (the `scope_ok = True # E1.2 fills this in` seam E0.1 left). **Region/Cluster Manager are NOT new roles** — `site_admin` + an org-unit grant, because the vocabulary is fixed (a test asserts no route demands a permission outside it). Ancestors are structurally non-authoritative: `contextual_unit_ids` is a separate field no decision method reads. **Two defects the slice found in itself:** the delegation ceiling had no effective caller (agent creation was tenant-gated, so it could only cap a tenant-wide creator who can delegate anything — now object-gated per scope row, and `org_unit` added to agent bindings so a region owner can build a region agent); and the strict preflight would have counted `legacy_open`'s SYNTHESIZED tenant grant and locked a grantless tenant out — `Grant.synthesized` now marks it. `site_id` sits outside `_chain_payload`, proved three ways. Executable matrix: `ROUTE_CONTRACT` + a route-contract test that fails the suite on an undeclared route + a generated persona sweep. SM untouched (site identity, no second resolver). Console Access Scope page. Live on real PostgreSQL with 6 Keycloak identities under strict: site admin reads across two ancestors, sibling cluster 404, ancestor readable but mutation 403, operator's approval at another site 403, platform super admin with no grant sees nothing, cluster auditor 6 audit entries vs tenant auditor 105, chain valid. 2896 → 3009 tests |
| 2026-08-30 | **E1.3 multi-site Site Manager shipped** — E0.2 made every CC-facing READ site-resolved; every WRITE and all 20 SM routes still resolved one config name, and `Ingest.register()` memoized it on the instance, so two sites on one SM would have put EVERY device from both into one site row. **D1 (ratified): site identity is authoritative, never agent-declared** — `site_enrollment_tokens` (hash-only, unique so one secret can't resolve to two sites, shown once), unknown/revoked/expired/inactive-site all refused and audited with ZERO device rows created, no credential on a multi-site SM refused, single-site SM unchanged for compatibility; `AgentRegistration` gains `enrollment_token` and deliberately has NO site field. A site change is an explicit re-enrollment — the upsert happened not to rewrite `site_id` so the outcome was right BY ACCIDENT; now refused with a reason. **D2 (ratified): the site is the normal safety boundary** — `sm_stop_switches` with three non-interchangeable scopes (tenant / site / site_manager emergency, the last needing a typed confirmation); lifting the emergency does not resume a site an operator stopped. **Defect found: the SM stop switch was an in-memory boolean** — not per-site and NOT PERSISTED, so a restart silently resumed autonomy an operator had halted. `execution_permitted()` takes 10 inputs and treats an UNEVALUATED one as refusing, which is what makes "autonomy is a ceiling, never execution authority" structural. Correlation is per site (`on_onset` uses the device's own site and refuses one with none; `sweep` loops sites) — left alone it would have produced shared-power incidents spanning estates that share no power. SM migration **0009**; `audit_log.site_id` outside `_chain_payload`. No second RBAC resolver: asserted over the IMPORT GRAPH. Live with two SM processes on real PostgreSQL: site-bound registration, 0 raw secrets stored, unnamed read 400, cross-site 404, site-1 halted while site-b ran, halt survived a restart, SM-02 `site_resolved=false` and 0 devices for both A and B, both chains verify. 3009 → 3064 tests |
| 2026-08-30 | **E1.4 tenant realm boundary shipped — E1 COMPLETE**. `TenantService(session)` was built with NO keycloak_admin at all four API call sites, so `if self.keycloak:` was always false and `KeycloakAdminClient` — 400 lines shipped in R2b — was **never instantiated outside its own module**; only the mock was, and only in tests, which is why they passed. Creating a tenant returned **200 with `keycloak_realm: null`**: no realm, no roles, no client, no owner. **Eighth instance of the house bug, and an entire subsystem.** Fixed: real client constructed in `AppState` and injected; creation **fails closed** (rolled back, audited `tenant.realm_provision_failed`); reusable idempotent `provision_realm` + `POST /tenants/{id}/provision-realm` for tenants predating E1.4; Console migration **0004** makes `tenant.keycloak_realm` unique+authoritative and `_resolve_tenant_id` stops consulting the slug; CC repointed to `tenant-demo` and its config now **refuses to boot pinned to the platform realm**. **Three more defects found:** (a) a realm migration silently orphaned EVERY scope grant — subjects are realm-scoped, so under strict the tenant was **locked out including its administrator**; CC migration **0012** adds `cc_scope_grants.realm`, the resolver ignores foreign-realm grants, and the enforcement read reports `locked_out` because a silent lockout looks exactly like correct strict mode; (b) a custom bundle could WIDEN a role (OR-ed, so a bundle naming `tenant.manage` handed it to a viewer) — bundles now intersect, E1.2's subset rule applied to bundles; (c) provisioning had a 2s timeout and httpx timeouts stringify to EMPTY, so the error read `"failed: "` — 10s and typed errors now, plus `ensure_realm_roles` reconciles a half-provisioned realm. Live: `tenant-rival` created end-to-end through the real API; platform realm stripped to platform roles only; platform→CC 401, tenant→CC 200, rival→demo 401/403, master-realm 401; **the E1.2 matrix re-runs identically on the tenant realm**; a real bundle on a real tenant user yields `fleet.view` only. E0.3's test-proven-only gap CLOSED. 3064 → 3096 tests |
| 2026-08-30 | **E1 gate GREEN from a fresh teardown** — the compose gate rebuilt for the tenant-realm topology (52 steps) and run on a wiped stack with real Keycloak + PostgreSQL. Every CC-facing token is now a TENANT-realm token; the gate's platform-realm operator/auditor creation was deleted (it re-added tenant roles to the platform realm right after the step asserting their absence). **Five defects the gate found that no unit test could:** (1) `seed-demo.sh` used ONE platform token for both Console and CC, so site registration 401'd once CC validated the tenant realm; (2) the synchronous approval path never wrote `agent_proposal.dispatched` — only the background loop did — so every human-approved agent action left the chain reading created→settled with the dispatch invisible; (3) **E1.3 broke Heartbeat/verdict on a multi-site SM** — both went through the ambiguity-refusing `_site()` instead of the device's own row, so every heartbeat threw and no incidents or proposals followed (it surfaced as silence, not error); (4) **migration 0010's backfill only rooted tenants that existed when it ran** — a site registered afterwards had no organizational path at all, so `ensure_root` now runs at site registration; (5) the gate injected psu target `PSU1` where the simulator wants `PS1`, and the simulator returns 200 "injected" when nothing matched. Also fixed: `lookup_tenant_id` works from both planes (a tenant identity reads `/api/me/tenants`, not the platform-only admin listing), placement fail-closure is asserted per plane, and long-run token expiry is re-minted in the tail. **E1 COMPLETE.** 3096 → 3104 tests, gate GREEN, tsc clean, UI build green |
| 2026-08-31 | **Capability Registry shipped — Amendment A17** (D1 + D2 ratified by Vinod). The platform could state an action class's risk, preconditions, blast radius, approval policy and autonomy grant, and could NOT state whether any code existed to execute it. Two classes had none: `INTERFACE_RESET` (never implemented on any protocol) and **`CLEAR_COUNTERS` — found by this slice** (R6 correctly refused to fake a counter clear SONiC exposes only over CLI, but nothing upstream knew, and the Operational Agent's condition table maps an interface condition straight to it, so a proposal would be made, approved by a human, dispatched, and refused at the node every time). **The node is the only authoritative source:** `supported_actions()` on each protocol declares implementation reach, kept honest by a test that executes every declared action and refuses every undeclared one; `declare()` intersects reach with the node's own allow list; three sets ride the wire because "no code for it" and "not permitted here" are different problems. New additive proto `AgentRegistration.capabilities_json` + `FleetDevice.capabilities_json` → `devices.capabilities` (SM **0010**) → `cc_fleet_cache.capabilities` (CC **0014**), both nullable with NO backfill: **unknown is not zero**, and backfilling an empty declaration would strip every bound class from any fleet that upgraded CC before its agents. `GET /api/capabilities` + `/devices/{id}` at `fleet.view` (D2 read-split, E1.2-scoped, in `ROUTE_CONTRACT`, zero mutations). One new platform declaration, `ACTION_REVERSIBILITY` beside `ACTION_RISK` — a genuinely different axis (`SEL_CLEAR` is risk `low` and permanently destroys the log), reported only. Consumers refuse what they cannot execute: A0 binding refuses a platform-unimplemented class and a class with provable zero reach in the agent's own scope; the evaluator will not propose into a device that declared it cannot run it; `agent_view` reports capability BESIDE the disposition, never merged into it. **Two defects the slice found in itself.** (1) On the live stack: the zero-reach check first used the E1.2 site-based repository filter, so a `device_class`/`device` scope returned no devices and the binding was waved through while the evaluator would never act on it — now `resolve_scope`, the same function the evaluator uses, so there is one notion of "in scope". (2) **In CI, and the more important one:** the refusal stood on `effective` (reach ∩ the node's allow list), so it refused on POLICY. The compose gate caught it — A0+A1 binds SEL_CLEAR to a demo node whose allow list does not carry it, deliberately, to prove the node's refusal is final and becomes attributed evidence. The module's own docstring said "reach is NOT policy" while the check did the opposite: an allow list is mutable operator policy and A17.1 already assigns "may it happen right now" to the node, so refusing on it promoted a mutable setting into a hard CC constraint and would have made it impossible to configure an agent ahead of a config rollout. **Refusals now stand on `implemented` only**; "bound, capable, permitted nowhere" is REPORTED by name on the agent view instead (A17.7 rewritten, pinned by tests in both directions and by a new gate step). **D2 honoured:** `execution_permitted()`'s sixth `capability` slot and `Agent._authorize_execution` are untouched; the node allow list remains the final execution authority. Follow-ups named in A17.8: **capability execution gate** and **skill-recommendation validation** (the fifth declaration site). Live on a wiped stack with real Keycloak + PostgreSQL: real redfish and gNMI agents declared themselves, both unimplemented classes reported over the wire, all three block reasons distinguishable on one device, bindings refused at platform and scope level across all three scope types, refusals left nothing behind, Console proxy 200. 3104 → 3234 tests, gate extended with A17 steps |
| 2026-08-31 | **S6 Campaigns shipped — Amendment A18** (D1/D2/D3 + planning contract ratified by Vinod). Generic capability orchestration across an estate, **not firmware campaigns moved to CC**: all 14 ActionTypes through one path, differentiation from the existing contracts only. Campaigns existed at the SM — single-site, firmware-specific, `require_site_token`, with a private `approved_by` field and no capability/autonomy/RBAC interaction. S6 is CC-resident and consumes the E1.1 tree, E1.2 resolver, Capability Registry, S5 autonomy, E0.1 approval ledger, DispatchAction, audit chain, outcome writer and learning loop. **The gap implementation found:** approval is per site-wave, a site-wave is a device set, and wave membership needs fault domains that exist ONLY at the SM — so CC could name "site A wave 1" but not which devices, leaving an approver signing a blank cheque. Ratified answer: a **read-only deterministic planning contract** (`PlanCampaignWaves`) that writes nothing (proven by table snapshot), returns exact membership + a domain COUNT (never domain identities), and a plan hash. `plan_waves()` is never duplicated into CC and no topology is stored there. **Approval binds to a plan**: the subject is a digest over campaign+version+site+wave+exact device set+plan hash, so a stale approval structurally cannot address a new subject; plans are immutable and superseded, never updated. **APPROVED ≠ EXECUTABLE ≠ EXECUTED**: a changed plan REFUSES the wave (never narrows it), capability/policy may only narrow, a newly capable device is never added. Q1 all site-wave subjects raised at submit (autonomous raises none); Q2 explicit `/advance` **and** a durable reconciliation loop, neither an execution engine; Q3 a halted site voids its own later authorizations, audited, and never another site's. CC migration **0015** (7 tables; dispatch ledger keyed on plan_hash). **Four defects found, each by a different mechanism:** reconciliation audit — scope rules lived in `params`, which the dispatcher ships to nodes, so governance selection would have become an execution payload (now `cc_campaign_scopes`, deliberately not `cc_scope_grants`, which is an authorization grant); integration test — a wave whose every dispatch was refused was marked *completed*, so a site walked over work that never ran; live stack — a hand-written delegation ceiling passed `org_unit_id` to a function taking `org_unit_path`, 500 on the headline org-unit case (now reuses `_scope_rule_within`, the one implementation); live stack — a missing import made approving a wave 500, because every part of the decision was unit-tested except *calling* it. Live on the real stack: region-scoped campaign, preflight excluded the gNMI switch for a server-only class BEFORE dispatch, CC stored zero topology, approval bound to the plan hash, advance refused an unapproved wave, repeated advance did not re-dispatch, node executed for real and both SUCCESS and FAILURE came back attributed to `campaign:<id>@v1`, wave settled from the outcome, campaign `completed`, chain verifies. **Three levels of status stay distinct and are never collapsed:** CAMPAIGN = aggregate orchestration result, SITE = independent execution result, WAVE = authoritative per-wave approval/execution result — proven live as campaign `halted`/`partial_success` with site A HALTED (its remaining waves VOIDED) and site B COMPLETED, on two real device-bearing sites served by ONE Site Manager. The halt was real, not simulated: site A's node permits only IDENTIFY_LED and refused COLLECT_DIAGNOSTICS by its own allow-list authority. **Fifth defect, found building that proof:** agent `_coerce` handled bool/int/float but not list despite its docstring, so `HARKENIQ_ACTIONS_ALLOW_LIST` became a list of CHARACTERS and the node refused every action — silently, because refusing is fail-closed — and `allow_list` is half a node's capability declaration, so such a node declared an effective set of nothing while looking correctly configured. 3234 → 3322 tests |
| 2026-08-31 | **A2 correctness slice shipped — Amendment A19** (D1/D2/D3 ratified by Vinod; recorded in spec §9 + design doc §22 BEFORE the code, per change control). A0+A1 made the Operational Agent an object; A2 makes it a **product**: configurable, examinable before it is switched on, budgeted, pausable, explicable afterwards. Activation becomes a governed transition — CREATE→CONFIGURE→PREFLIGHT→ACKNOWLEDGE→APPROVAL (where required)→ACTIVATE→RUN→OBSERVE — and switching an agent on without a stored preflight for **that exact configuration version** is refused; A0's two ad-hoc checks became two of the preflight's twelve dimensions, so the Console and the gate cannot disagree. The preflight is a contract, not a checklist: twelve dimensions × four verdicts, BLOCKED dominant, **UNKNOWN first-class** (A17.4 — a fleet mid-upgrade is unknown, not incapable), stored immutably and superseded rather than updated, and it **confers nothing**. **D1** activation approval is DERIVED — required only where activation confers real unattended execution, so a propose-only agent is switched on without ceremony; the subject is a digest over (agent, version, unattended set) so an edit structurally voids it, on the **existing** E0.1 ledger as a fourth origin. **D2** the budget counts EXECUTIONS and caps only unattended work. **D3** an approved proposal keeps its version and is not a guarantee. Skills become real — the four pieces E0.3 named when it refused the kind rather than leave it inert (Console skill-by-id on the existing internal channel, CC fetch with `parse_skill` still the untrusted-YAML boundary, per-device `InstallSkill` targeting, install-on-activation), per-device ledger + dedup + skip-with-reason + audited. CC migration **0016**. **Five defects the recovery review found in A2's own first pass, and the pattern is the house one — declared, modelled, migrated and READ, but never written or never called:** (a) `_activation_decision` counted any single record as approval, so a tenant configuring `required_approvers=2` got **single authorization for activation** — E0.1's own defect at a fourth origin, and the **ninth** instance; it now calls the one completion rule, pinned structurally as well as behaviourally; (b) `activated_version` had no writer, so `configuration_drifted` computed `0 != 1` and **every active agent reported drift from the moment it was switched on** — an actively wrong answer, not an inert field; (c) `unattended_permitted` had no production caller — `dispatch_decided` shipped every approved proposal, `autonomous_grant` included, so D2 was reportable and unenforced; withheld work now returns to the human queue rather than failing (R3a's propose-not-deny shape); (d) budget and pause were columns with **no way to set them**; (e) `install_bound_skills` was complete and invoked by nothing. **Found by the mandated real-PostgreSQL step, not by unit tests:** an agent active before A2 carries `activated_version=0` against `version≥1`, so the naive rule shouts DRIFT at every existing agent the moment a customer upgrades — and backfilling would assert the opposite unchecked fact, so `activation_provenance` reports recorded/unknown/inactive from ONE implementation both readers ask (the rule previously had two copies). The stale E0.1 fixture was fixed by **creating the agent, not weakening the D3 gate**. Verified: 3447 tests, tsc clean, UI build green, compose gate GREEN on a clean stack, and on real PostgreSQL the full chain to 0016 plus a simulated pre-A2 `0015→0016` upgrade with existing data and its downgrade. **A2 is NOT complete:** the completion slice owns the Operational Agent Console surface, live A–K acceptance, and the live dual-approval activation proof (which needs the tenant autonomy level raised, so it belongs with A–K). 3322 → 3447 tests |
| 2026-08-31 | **A2 COMPLETE — completion slice shipped** (implements A19; no new amendment). The Console answers the twelve questions a customer has about an agent — who, where, what it can do, what skills, how autonomous, what approval, what budget, what safety, is it ready, why blocked, which configuration is RUNNING, what is it doing now — by **rendering contracts Central Command already composes**, recomputing none of them: a page that reached its own verdict could show an operator something different from what the activation gate enforces. The Activate button reflects the stored contract and the server refuses independently, in its own words. Governed activation replaces the bare status write (preflight → acknowledge → approval where required → activate), plus runtime health, budget configuration, per-device skill delivery, and the activation origin rendered in the one approval queue. **The gap building the page exposed:** an activation waiting on a human appeared NOWHERE in the approval queue — the decision path was complete and correct, but the only way to reach it was to be handed the subject digest from the agent's own page, so "there is no second approval queue" was true of the ledger and false of the product. Activations are now a third origin in the same list, same envelope, same progress block, same decision endpoint; a decided one leaves. **Second defect, found building the acceptance:** D2 consumption was keyed to `op-agent:<id>@v<n>`, so editing a DESCRIPTION bumped the version and reset a spent budget to zero — the one control bounding unattended work was refilled by the most routine edit there is. Attribution still names the version (D3); consumption belongs to the AGENT across versions. **Third:** activation's scope check fell through to the tenant question by accident on an empty site id, refusing a site-scoped operator with "this  is outside your authorized scope … over the site it targets" — about a subject that targets no site. The behaviour is right (an agent's reach spans whatever its scope names) and is now asked explicitly and refused in those words. **Live A–K on real PostgreSQL + Keycloak, exit 0:** propose-only activates with no approval; unattended is refused, queued, refused for a site-scoped operator, approved by tenant authority, activated, and leaves the queue; no-reach blocked and refused; capability/policy/UNKNOWN stay three answers; budget survives an edit; drift after editing an active agent with the preflight going stale; proposals retain their originating attribution; per-device skill ledger; four lifecycle actions attributed to a named person on a chain that verifies. 3447 → 3464 tests |
| 2026-08-31 | **A3 machine identity shipped — Amendment A20** (D1 option B + D2 hard ceiling ratified by Vinod; recorded in spec §9 + design doc §23 BEFORE the code). An Operational Agent had never held a credential: it is a row evaluated by a CC-resident loop calling the governance composers in-process, and its identity was an attribution string. A3 gives it **one Keycloak client-credentials service account per LOGICAL agent** in the tenant realm, 1:1 with `cc_operational_agents.id` (CC migration **0017**). Keycloak is reused, not replaced — no second identity provider, no token service. `api_keys`, a full credential lifecycle whose verifier `get_by_hash` **has no production caller** and therefore authenticates nothing, is retired rather than adopted. **The defect A3 was designed around:** `load_agent_scope` passes `role_permissions=["*"]` and says why — *"it does not call the HTTP API, the CC-resident evaluator does"* — which is exactly the premise a credential removes. Verified: resolved that way an authenticated agent satisfies **every** route guard in the platform, `action.approve` included; **it could approve its own proposals.** So the ceiling is a HARD INDEPENDENT CONSTANT and the effective set is `A0 read bindings ∩ MACHINE_PRINCIPAL_CEILING = {fleet.view, incident.view}` — deliberately not "whatever today's bindings imply", so no future binding can widen it (E1.4's bundles-intersect rule, applied to a machine principal; pinned by a test that grants EVERY binding then sweeps the whole 22-permission vocabulary). Secrets are **never stored at CC** — there is no column they could be written into, asserted by the migration test — and **CC's status row is authoritative on every request**, so revocation beats an otherwise-valid 300s JWT immediately. Provisioning goes through the **existing** CC→Console internal channel rather than giving a tenant-plane service Keycloak admin credentials. Identity binds to the agent not the version (no re-credentialing on edit); retiring an agent retires its identity; a revoked identity refuses already-approved work through the `agent_identity` dispatch slot that already existed, so **approved proposal version ≠ guaranteed execution** is unchanged. **A12.1 is NOT amended:** platform staff get aggregate counts only, on their own endpoint — `/usage-events` feeds `MeteringService` and an operational signal in a billing ingest could corrupt invoicing. **Two things the slice found in itself:** the catch-all `/{agent_id}/{transition}` route was registered before the new identity routes and swallowed `POST .../identity` (a route that existed, was guarded correctly, and was unreachable — moved last, with a comment saying why); and `keycloak_sub` was `String(64)`, below the platform's own 128 minimum for a subject column and unguarded by the identity-width invariant, which now covers it and five other principal columns (third brush with the sqlite-hides-VARCHAR-truncation trap). Live on real Keycloak with real `client_credentials`, exit 0: identity issued, agent authenticates, both ceiling reads 200, an unbound read 403 (the intersection is per agent), **the agent's own token 403 on `action.approve`**, every mutation and `audit.export` 403, it cannot credential itself, rotation leaves the old secret 401 and the identity unchanged, an unexpired token from a revoked identity 401, retired agent 401, five identity audit actions with no secret anywhere in the log, chain verifies, aggregate carries no per-agent detail and creates no metering record, platform-realm token still 401 at CC. 3464 → 3501 tests |
| 2026-09-01 | **A4 governed capability expansion shipped — Amendment A21** (option A ratified by Vinod: expand ADDRESSABILITY, not autonomy; recorded in spec §9 + design doc §24 before the code). **The gap the inspection measured:** the platform implements 12 of its 14 action types, an agent could propose 6, and one of those six had no executor at all — so **seven implemented, governed, node-executable capabilities were invisible to every agent**. Not forbidden, not fenced, not denied; unreachable, because nothing mapped a condition to them. **A whole subsystem was dead:** `interface` mapped ONLY to `CLEAR_COUNTERS`, which no executor implements, so A17's zero-reach rule refused the binding and **a switch-scoped agent had no proposable action at all** — R6 shipped the gNMI network actions and A0's condition table never learned they existed. **And `execution_permitted()` had no production caller:** A17.8 recorded its `capability` slot as unsupplied; the broader truth is that the ten-input fail-closed gate E1.3 shipped was referenced only by its own module and by tests while the runtime used hand-written sequential checks alongside it — the tenth instance of the house pattern, and the one A17.8 said had to land first. **What shipped:** `REMEDIATION_CANDIDATES` became `cc_capability_catalogue` (CC migration **0018**, tenant-scoped, seeded so no tenant's behaviour changed, both tenant shapes proven on real PostgreSQL and idempotent across a re-upgrade); the interface subsystem repaired to `INTERFACE_ENABLE`/`INTERFACE_DISABLE`; `POWER_CAP_ADJUST`, `POWER_CYCLE` and `CONFIG_RESTORE` made addressable on conditions that genuinely exist (`config:` and `os:` are real `sensor_id` prefixes — a test asserts the whole seed vocabulary rather than trusting it); `evaluate()` takes the catalogue as an INPUT with **no hardcoded fallback**, because a default would be the old constant surviving as a shadow; and the SM's dispatch checks now run THROUGH `execution_permitted()` with `capability` supplied from the device's own declaration — same checks, none added, none removed, with `CC_INPUTS`/`SM_DISPATCH_INPUTS`/`NODE_INPUTS` asserted to **partition** the ten exactly so no input can be dropped by the split or owned twice. **Deliberately left alone:** `FIRMWARE_UPDATE`/`FIRMWARE_ROLLBACK` stay campaign-only (an agent does not propose a firmware update on a fault); `CLEAR_COUNTERS`/`INTERFACE_RESET` stay unimplemented and truthfully reported (A4 changed their visibility, not their status); and **nothing was mapped into the autonomy ladder** — every newly addressable class requires a named human, because evidence of effectiveness is not authority to execute unattended. Also: `implemented_actions` moved to the shared module rather than being copied into the SM — three services ask it now, and a second copy would be a second answer. Live, exit 0: catalogue served with the interface subsystem alive, five classes newly addressable, both unimplemented classes refused on CAPABILITY with their own reasons, firmware refused with ITS reason, every newly addressable class still requiring approval, operator read-200/write-403, catalogue rewrite audited on a verifying chain, the proposable set following the catalogue rather than a constant, and `execution_permitted` proven to be the dispatch path. 3501 → 3536 tests |
| 2026-09-01 | **A5 canonical agent interaction contract shipped — Amendment A22** (D-A/D-B/D-C ratified by Vinod; recorded in spec §9 + design doc §25 BEFORE the code). A5 was scoped as "MCP Tier-1"; the inspection reframed it. The gap was neither tools nor locality: **you could not ask an agent what it would do about an incident without it doing it** — `run_once` had no trigger, evaluation was cadence-only at 120s, and the "validation/simulation" pair A2 was assigned shipped only its validation half as preflight, the simulation half silently dropped like `/api/v1`. **The defect A5 found in A4, which I shipped:** four classes A4 made addressable can never succeed — the evaluator emits `params={"reason": …}` for EVERY class while `IDENTIFY_LED` needs `target`, `CONFIG_RESTORE` `attributes_json`, `POWER_CAP_ADJUST` `target_watts` and `INTERFACE_*` `interface`; there was **no action-parameter schema anywhere in the repository**, and each proposal was made, approved by a human, dispatched and refused at the node. Governance held (the node refused and it became attributed evidence) but the platform promised what it could not keep. **The parameter contract** is now ONE platform declaration beside `ACTION_RISK`/`ACTION_REVERSIBILITY`, validated BEFORE a proposal exists, with `source` recording what can supply a value — `unavailable` is first-class, so `POWER_CAP_ADJUST` and `CONFIG_RESTORE` are truthfully reported as implemented-and-unproposable with the missing input NAMED (no power policy exists; drift detail is agent-side). `skills/*.yaml` had carried its own `params:` block since R1 — a **fifth** undeclared schema site — and `parse_skill` now validates it against the platform's answer. **The component identity was not missing, it was discarded:** `sensor_id = f"{skill.target}:{sensor.name}"` and the SM did `sensor_id.split(":", 1)[0]`, throwing the remainder away, so CC held no drive bay or port name for any device; it now rides `FleetIncident.components_json` → `cc_incidents.components` (CC **0019**, nullable, NO backfill — `[]` would assert "nothing affected", which nobody checked). **`govern_proposal()`** is the one verdict function A6's ingress will call, with the CC-resident evaluator as its first production consumer — which honours A20.10 literally and resolves the spec-vs-design contradiction without amending either. **Dry-run** returns what an agent WOULD propose, writes nothing (proven by table snapshot), reports what it withheld and why, and needs NO ceiling change: `fleet.view` is already in `MACHINE_PRINCIPAL_CEILING`, so an agent may dry-run ITSELF, gated at the object level. Drafted as a POST; the route contract's own "a mutation must resolve its target to a scope" refused it and the **verb** was what was wrong. **Five defects fixed:** `/api/attention/` was declared READ_SCOPED and applied no scope (and is the one read EVERY agent must hold); `legacy_open` synthesizes tenant-wide reach for a grantless principal, inverting A0's "no scope rows = no devices" — now REPORTED before enforcement via `/scope-enforcement/impact`, which admits it cannot enumerate a realm's principals; the router duplicated `governance.load_attention` with a `band` filter that ran BEFORE ranking while its own comment said rank means first in the tenant; autonomous dispatch skipped the CC gates so paused/retired/revoked agents still dispatched, and the human-approved path asked nothing at all; and `load_agent_scope` resolved with `role_permissions=["*"]` so the scope answered `permits("action.approve")` with **True** — removed at the source, not patched at call sites. The preflight's `capabilities` dimension now reports a binding that can never fire. D6–D10 are named follow-ups, not silent drops. 3554 → 3630 tests |
| 2026-09-02 | **A23-1 Enforcement shipped — Amendment A23** (D1–D4 + three follow-on decisions ratified by Vinod; recorded in spec §9 + design doc §26 BEFORE the code). The validation report found two failure classes: **(A) synthesis escalation** — proven by executing the resolver, an EXPIRED, REVOKED or deleted-org-unit grant made the principal tenant-wide with full role permissions under `legacy_open`; **(B) declared-but-not-enforced scope** — `ROUTE_CONTRACT` lived in a TEST FILE, no runtime consumed a treatment, the persona sweep asserted 403-vs-not-403 only. A23-1 is stage B. `harkeniq_cc/route_contract.py` is now runtime code; a **consumption census** (source-level: a handler that accepts `scope` and never reads the name fails the suite by name) found **25 violations** the earlier reading had listed 21 of; a generated **narrowing sweep** (strict site-scoped personas × every read route × an estate seeded with a distinguishable out-of-scope object of every kind — site, unit, device, incident, campaign, agent, proposal, grant, approval subject, outcome, warranty tag, candidate skill, learned signal) and a generated **mutation probe** (narrowed tenant owner × every mutation × an out-of-scope target; 403/404 only, 422 counted as unproven). Fixed: campaign acknowledge/submit/cancel/advance were unscoped execution controls (now gated on every scope rule); campaign waves/targets/sites/get narrowed and a DRAFT (no site rows) judged by its rules instead of shown to everyone; **campaign preflight unioned the CALLER's reach into the target set** via a second wildcard, realm-less resolve — a one-site campaign by a tenant owner targeted the whole estate — now `expand_rules_to_site_ids` ∩ caller scope, proven live; operational-agent list/get/proposals/preflight/runtime/identity/dry-run visible by scope rule (404 otherwise), catalogue sites scoped, acknowledge under the ceiling; `/api/scope-grants/` narrowed to grants the caller could have made; `/api/predictive/risk`, `/api/firmware/exposure`, `/api/warranty/`, `/api/learning/candidates`, `/api/learning/signals`, `/api/outcomes/patterns` re-declared READ_SCOPED and made so; `/api/autonomy/` and the agent view narrow every list that NAMES a site (the sweep caught `learning[].scope_ref` after the first pass); CVE-feed and warranty imports tenant-gated; approval records absent for an out-of-scope subject. **Realm:** `validate()` short-circuited on an EMPTY realm and `app.py` fell back to the platform realm — secure mode now refuses, fallback deleted. **Metering** pinned scope-free by test (A23.5). Gate extended with four A23-1 steps. Deferred to A23-2..5 by design: actor_ref, last-admin, vanished target, delegation authority, synthesis, strict birth. 3598 → 3621 tests (3543 unit passed, 1 skipped) |
| 2026-09-02 | **A23-2 Identity shipped** (implements A23.7). `cc_audit_log.actor` was written in three forms — Keycloak subject (19 sites), email-or-subject (10), and an email handed down as `decided_by`/`actor` (4 + lifecycle) — and the A22.10 migration census compared those display strings to subject-keyed grants, so a granted person recorded by email read as UNGRANTED. Now: `actor_ref` (CC migration **0020**, nullable, `(tenant_id, actor_ref)` index, OUTSIDE the chain payload like `site_id`; NO backfill — a historical row stays exactly as written), ONE helper `actor_of()` (human → subject, machine → agent id, attribution key → agent id across versions, campaign → versionless ref, email → None), every API audit write threads `actor_ref=actor_of(user)` (pinned by a scanner test), `append()` derives what it can for attribution-only writers, dual-form readers (audit API returns both; the actor filter matches either), and a census that resolves observed actors through the stored ref → the helper → in-repo evidence pairs (approval records, group members, the ledger's own rows) and reports the rest as `unresolved_legacy_actors`. Live on real PostgreSQL: the gate rewinds the live DB to 0019 and upgrades with rows present — all NULL, index present, chain valid, legacy rows readable by display string, the owner not reported as ungranted. 3621 → 3643 tests |
| 2026-09-02 | **A23-3 Recovery + delegation shipped** (implements A23.6, A23.8, A23.9). Traced on `a69fc42`: revoking the last tenant `role.manage` grant was unguarded, and so was OVERWRITING it — `POST /api/scope-grants/` revives an identical grant through the repository and replaces its role, subset and expiry, so "expire the last administrator" was one ordinary create; only the strict flip had a preflight. `can_delegate` had zero production callers, create checked REACH only, and the recorded `role` column was never read by the resolver, so a tenant owner narrowed to one site could hand out a full `tenant_owner` grant there; self-grant was refused nowhere. A grant to a deleted org unit was DROPPED from the resolved list — the empty list `legacy_open` synthesizes on — and a site grant to a missing site still answered `covers_site` true. Org-unit delete never consulted grants; stale-realm grants counted toward the admin preflight; retiring an agent left its rows active while rebinding hard-deleted them; two concurrent last-admin revokes both passed a naive count. Now: `harkeniq_cc/grant_integrity.py` holds the rules at the boundary every mutation crosses. **Inert, not absent** — a vanished target (unit, site) stays in `ResolvedScope.grants` as `inert` with a reason, covers nothing, delegates nothing, blocks synthesis, and is reported by `/me` and the list (`target_status`, `effective`). **The recorded role is a ceiling** (`token ∩ ROLE_PERMISSIONS[role] ∩ subset`; a row with no role is unchanged) and a human grant must name a tenant role. **Delegation = reach ∧ authority per permission on the exact target** via the grantor's own `permits`; a narrowed owner cannot regain a withheld permission by naming a broader role; **self-grant refused by identity** (subject or own email) before any scope question. **ONE counting function** `count_tenant_admins` serves revoke, overwrite, reassign and the strict flip under a transaction-scoped per-tenant advisory lock; refuses ≥1→0 on ACTIVE admins and on PERMANENT (unexpiring) admins, so a future-dated expiry on the last admin is refused too; the caller's own status is recomputed from the LOCKED row and their token, not the pre-lock scope; stale-realm rows never count; every refusal audited (`scope.revoke_refused`/`scope.grant_refused`/`scope.enforcement_refused`/`org_unit.delete_refused`). Org-unit delete refuses under any ACTIVE grant (user or agent) naming the grant ids; new atomic `POST /api/scope-grants/{id}/reassign` gated on both targets. Agent scope rows revoke and revive instead of delete; retire revokes them. No migration. **Deferred by the spec's own assignment:** revoked/expired-under-`legacy_open` synthesis (A23-4, pinned by two strict xfails), strict birth (A23-5). Live on a wiped stack with real Keycloak + PostgreSQL: last-admin revoke 409, self-modification 403, two admins revoking each other CONCURRENTLY → exactly one 200 and one 409, site-scoped delegation in/out/tenant 201/403/403, narrowed grantor refused `site_admin` with `site.manage` named and allowed `viewer`, unit delete 409 → reassign → 200 with the refusal audited by grant id, an orphan-only principal under BOTH postures: `tenant_wide` false, zero devices, inert `org_unit_missing`; chain verifies. **Finding for A23-5 (found by the gate):** the gate had bootstrapped its first administrator by an owner SELF-grant under `legacy_open`, which A23.6 now refuses — the first tenant grant is a two-person act, and a strict-born tenant (A23-5) has nobody able to make it, so A23-5 must seed it at provisioning. 3643 → 3699 tests |
| 2026-09-02 | **A23-4 Legacy synthesis correction shipped** (implements A23.10). Traced on `4b9d2f2`: `list_for_principal` discarded revoked rows before the resolver saw them, `resolve()` discarded expired, unknown-typed and permission-less rows with `continue`, and the branch `if not grants and legacy_open` had no principal-type check — so revoked, expired, invalid and (for agents) absent grants all read as NEVER GRANTED and were handed a synthesized tenant-wide grant; an A5 test asserted the agent half as present by name. Now: `ScopeGrantRepo.grant_evidence` (every row that ever named the principal — revoked, expired, other-realm, alias-keyed) feeds `resolve(prior_grants=)`; evidence is counted BEFORE the lifecycle filters; synthesis requires human + legacy_open + no effective grant + NO evidence, and constructs a synthesized grant in exactly one place (structurally pinned). `ResolvedScope.previously_granted` + `synthesis` reason on `/me`. Aliases are authenticated only: the token's own email claim and the A23-2 recorded email↔subject pairs, moved to `identity_evidence.py` as ONE implementation the impact census and the loader both ask; exact match, evidence only, never authority. Agents: no synthesis under any posture; the impact report still names a scopeless agent. No migration. A23-3's two strict xfails flipped green and lost their marks. Live on a wiped stack with real Keycloak + PostgreSQL: never-granted tenant-wide with devices; revoked, expired and vanished-target principals `tenant_wide false`, zero devices; a machine token for a no-row agent reads zero devices; strict synthesizes nobody; chain verifies. **Deferred:** `can_delegate` uncalled (cleanup); A23-5 must seed the first tenant grant at provisioning. 3700 → 3738 tests |
| 2026-09-03 | **A23-5 Strict birth shipped — A23 COMPLETE** (implements A23.11; D1/D2 ratified by Vinod as **A23.14**, recorded in spec §9 + design doc §27 BEFORE the code). Traced on `f018562`: `TenantSettingsRepo.enforcement()` answered `legacy_open` for a MISSING ROW — the platform's most permissive posture, reachable by a decision nobody made — while `models.py`'s own docstring had promised "New tenants are born `strict`" since E1.2, four slices earlier. **Two questions A23.11 could not answer as written, and the implementation had to settle both.** (a) *Which tenants may a CC migration enumerate?* None authoritatively: CC has 39 tables and no tenant registry (`cc_tenant_settings` is keyed BY tenant, not a list OF them), the authoritative registry is the Console's `tenants` in another service and another database, and enumerating operational tables (the 0011 pattern) is inference that misses a quiet tenant entirely. Since CC is single-tenant software (doc 01 §7 — every request resolves `config.tenant_id`), migration **0021** speaks for the deployment's OWN configured tenant, pinned `legacy_open` (the posture the old default was already giving it — never silently converted to strict), preserving any explicit row of either kind, idempotent, downgrade-safe on its own `migration:0021` tag. **The trap the slice found in itself:** on a fresh deployment alembic runs 0001..0021 in ONE invocation, so an unconditional pin would hand EVERY new install `legacy_open` and strict birth would never once happen — 0021 asks the audit log whether the DATABASE has ever served anybody (a freshness probe, not a tenant enumeration) and pins only a deployment with history. (b) *What does a tenant born without an administrator do?* Today: becomes usable anyway — `admin_email` was optional AND owner minting warned-and-continued, so two ordinary paths produced an `active` tenant with no owner subject, survivable only while `legacy_open` synthesized reach for a never-granted human. `tenants.status` has exactly two values in the codebase and inventing a `provisioning` state would touch billing, `/api/me` and admin listing, so creation now **fails closed** through E1.4's own rollback path. **The first grant is a provisioning act, not a principal's:** `seed_first_grant` writes ONE tenant-scope `tenant_owner` row and refuses if the tenant holds ANY grant of any lifecycle state — deliberately NOT `ScopeGrantRepo.grant()`, which revives a revoked row and would let a deliberately removed administrator return outside the lifecycle, against A23.10. The subject comes from the Console over the EXISTING CC→Console internal channel (new `GET /api/internal/tenants/by-realm/{realm}/owners`, resolved by realm because that is the one identifier the two services agree on); an owner with a NULL `keycloak_user_id` is omitted, because a grant keyed on an email is a guess — CC then reports the tenant unadministered rather than seeding something it cannot authenticate. Attributed `system:tenant_birth`, audited once, inert thereafter; the seeded row is ORDINARY (counted by `count_tenant_admins`, protected by A23.8, its holder still cannot self-grant — the gate retires it with a real administrator to prove exactly that). **Migrated ≠ newly born:** a historical tenant keeps its pinned posture and is reported through `locked_out`; no synthetic administrator is invented. **The blast radius, measured not guessed:** flipping the default failed 308 tests across 34 files, of which only 15 asserted legacy synthesis and 293 were fixtures that had been acting as administrators who were tenant-wide *by synthesis* — i.e. the suite had been testing an ungoverned tenant. They now seed a real founding grant through the same repository seam production uses (shared `seed_tenant_admin`/`seed_tenant_people`/`seed_legacy` helpers), so the mainstream API suites run under the posture a real tenant has. `seed-demo.sh` reconciled: the Console-created owner IS the demo identity now, instead of a second Keycloak user the birth grant would never have named. 3738 → 3764 tests |
| 2026-09-05 | **A6-1 Governed external ingress shipped — Amendment A24** (D1/D2/D3/D4 + the A24.11–A24.16 addendum ratified by Vinod; recorded in spec §9 + design doc §28 BEFORE the code, twice). An external Operational Agent runtime has held a credential since A3 and could reason about itself since A5's dry-run; it could never SUBMIT. The inspection found three of the four pieces a first slice would build already existed and already worked for a machine principal — the credential, the token→identity→agent→tenant→scope binding, and dry-run — so the slice was re-cut around the one real gap. **A `POST /proposals` carrying an action is not implementable:** `govern_proposal()` is condition-driven, `resolve_action_params()` DERIVES parameters and never accepts them, `open_dedupe_keys` is a tenant-wide read structurally unavailable to an HTTP caller (A22.1), and a body able to carry `authorization_basis` is a self-signed execution order (A22.15). So **propose-by-reference**: the agent names a candidate CC already governed and showed it through the existing dry-run, and CC re-derives and re-runs the SAME `evaluate()` before writing — `candidate_ref` is a lookup, never a licence. **D1:** `proposal.submit` enters the vocabulary and `MACHINE_PRINCIPAL_CEILING`, and the ceiling does NOT grant it — effective = (read ∪ ingress bindings) ∩ ceiling, so an agent without an explicit `ingress` binding is refused though the ceiling permits the class; it is in no human role. **D3/A24.6:** `admit_proposal()` is now the ONE admission path (the evaluator moved onto it) under a transaction-scoped advisory lock, because inspecting the lifecycle showed `all_dedupe_keys()` returns EVERY key — admission is already 'one proposal ever per dedupe key' and a partial-unique over open statuses would encode a rule the platform does not use. **D4:** the rate window counts committed rows, because CC runs multi-replica and an in-process counter would be decorative. CC migration **0022** (`cc_agent_submissions`, `cc_agent_ingress_attempts`, `cc_agent_proposals.operation_key`; additive, no backfill, `dedupe_key` untouched). **Two independent red-team rounds, every claim reproduced before it was implemented, all nine correct.** Round one: the check-then-insert idempotency race (loser raised `UniqueViolationError` — a 500 on the retry the key exists to make safe); attribution used as operational identity, so two agents could hold two ACTIVE proposals for one physical operation — fixed by `operation_identity()` DERIVED from the parameter contract (`SRC_COMPONENT` addresses, `SRC_ANNOTATION` does not), never hard-coded; a non-atomic rate check whose replay branch returned BEFORE it; a 5MB body returning 422, i.e. fully parsed first; and `_dispatch_permitted` re-asking status/pause/retirement/credential and nothing about REACH. Round two: the byte ceiling held for ONE shape of three (chunked and understated `Content-Length` both 400, because a mid-stream disconnect is indistinguishable from a client hangup and FastAPI answered first) — now buffer/decide/replay, the app never invoked for an oversized body; authenticated refusals were free — `proposal.submit` moved INSIDE the handler after the meter, charged to the agent the TOKEN names; and a DENIED proposal fenced an operation forever, because `find_open_operation` reused `OPEN_STATUSES` — `IN_FLIGHT_STATUSES` added beside it, `OPEN_STATUSES` untouched so the per-agent rule still treats a denial as final (D16). **Found by the slice in itself:** a sixth defect nobody reported — `CapabilityCatalogueRepo.list_for_tenant` seeded read→if-empty→seed unserialized, so two concurrent requests both seeded and one 500'd (pre-existing; A6 is what lets an external caller reach it); my own A24.12 text said 'OPEN proposals only' while `OPEN_STATUSES` contained `denied`, contradicting its own intent; the A23-1 consumption census caught the new route declaring `OBJECT_GATED` while never reading scope; an `ingress` binding with a typo'd ref would have been accepted and mapped to no permission; and **three concurrency tests passed with their lock removed** — interleaving-dependent false negatives, now labelled scenario tests with deterministic ordering guards beside them. Live on real Keycloak + PostgreSQL (fifteen gate steps): no binding → 403 though the ceiling admits; 200KB body → 413 before parsing; 6 attempts metered with the replay among them; a second agent refused `operation_in_flight`; submission → human-gated proposal; replay → 200 same proposal; two keys one proposal; cross-agent 403; revoked identity 401 on an unexpired token. Merged `536cfd4`. 3764 → 3872 tests |
| 2026-09-05 | **A6-2 Machine status + outcome correlation shipped — Amendment A25** (D1/D2/D3 ratified by Vinod; recorded in spec §9 + design doc §29 BEFORE the code). A6-1 answered how an external agent submits governed intent; A6-2 answers how it follows what HarkenIQ did with it. **The slice leads with a correlation defect the inspection found, not with the read model:** Central Command has held an exact execution key since A1 — the SM writes `action_id="directive:<directive_id>"`, `FleetOutcome.action_id` carries it, `cc_outcome_history.action_id` stores it — and `list_outcome_dicts()` DROPPED the field, so settlement matched on device, action class, actor and a time window. Two dispatched proposals for one device and one action class could settle each other's outcome; the headline A1 journey test was green throughout because it only ever had one proposal in flight, and its fixture wrote `action_id="act-1"` — an id no directive path produces — so the end-to-end test was exercising the heuristic rather than the production join. Settlement is now exact or nothing: a proposal holding a directive id waits for its own outcome (unsettled and visible beats settled and wrong), the heuristic survives ONLY where no key could exist (empty `directive_id` — a fact about the row, not a guess about its age), and both joins are named on the audit entry and counted so the fallback retires by measurement. **D1 (historical receipt):** operational reads stay current-authority — no grant, revoked, expired or vanished scope never restores estate visibility — but the SAME logical agent may read the lifecycle receipt for its own submission after its scope narrows, carrying lifecycle facts and NOTHING about the estate; `submission_id` is not a bearer credential and cross-agent/cross-tenant is 404. **D2:** approver identity is never machine-visible — the block is built by NAMING what may pass, because exclusion-filtering leaks the next field somebody adds upstream. **D3:** six layers stay six (`approved` is not terminal — the per-agent budget can return it to `awaiting_approval`, and `PARTIAL`/`ROLLBACK` never collapse to a generic failure). Two pure reads at `fleet.view` (no new permission, no ceiling change); **G5 closed** — machine-self normalized across `get_agent`/`runtime`/`preflight`/`proposals`, which A24.5 had assigned here. Reads metered in their own bucket AND their own shape: a windowed counter (`cc_agent_read_windows`, CC migration **0023**) rather than a row per GET, which would have made the meter the largest writer in the system; polling never enters the audit chain; ETag only where nothing can change again. **Three things the slice found in itself:** `record_read_refusal`/`record_correlation` interpolated their argument into a metric name, so a caller could have put a tenant or agent id into an unauthenticated scrape endpoint (both reason sets now bounded); a structural test grepped a docstring that deliberately NAMES the excluded approver fields, testing prose rather than code (replaced with hostile input); and the persona sweep correctly refused two routes that declare `fleet.view` yet admit no human — answered with a `MACHINE_ONLY_ROUTES` set in RUNTIME code, checked in both directions so an exemption cannot quietly hide a route from the matrix. The correlation fix needed NO migration: `action_id` was already stored and already populated. **Pre-merge remediation (A25.9–A25.12), four findings from the final independent review, all the same shape — a decision correct where it was made and not carried to what came to depend on it:** (1) A25.5 opened five more routes to a machine and the PROJECTION stayed the Console's, so `get_agent` answered an external runtime with `proposal_dict` (executable params, raw evidence, `decided_by`, `directive_id`, dispatch internals) plus `created_by`/`activated_by`; the listing enumerated every OTHER agent; the preflight read carried approver emails through the activation block; and the identity read had NO self rule at all, so an agent could read another agent's credential row and the operators who issued and rotated it — now six allow-listed projections built by naming what may pass, two withheld sets (operator identity never, on any response; execution internals never on a lifecycle/status response, dry-run the one A22.2 exception), the binding catalogue refused to a machine outright, and a route/meter matrix asserted COMPLETE against the running router; (2) the machine proposal list cached approval COMPLETION under `(action_type, device)` while completion is read by `subject_ref = proposal.id`, so two proposals sharing an agent, a device and a class shared one answer decided by list order — only the policy resolution is cacheable and now only it is cached; (3) the self rule ran BEFORE the meter, so an authenticated cross-agent refusal was free, and the order is now authenticate → derive the canonical machine identity → account → decide about the target, with the bucket structurally provable to come from the token alone; (4) the meter committed the CALLER's session — the mirror of the `session.rollback()` defect the counter itself already had — and now owns a short-lived session of its own, proved by the seven-step isolation test on real PostgreSQL because in-memory sqlite is a StaticPool where no two sessions can be isolated at all. Merged `5fb38e8`. **One finding the sweep produced and that PR did NOT fix (A25.13, recorded for Vinod):** `GET /api/policies/groups/{id}` returns each member's email and Keycloak subject at `fleet.view`, which a machine principal holds — so an agent can enumerate the tenant's APPROVERS, the fact A25.3 exists to withhold. Pre-existing since A3 gave a machine `fleet.view`; a different router with human consumers this slice did not review. 3872 → 3974 tests |
| 2026-09-06 | **A26 governance visibility boundary shipped — Amendment A26** (A25.13; D-A..D-E ratified by Vinod; recorded in spec §9 + design §30 BEFORE the code). Closes the pre-existing HIGH A6-2's route sweep found and independent review confirmed: `GET /api/policies/groups/{id}` was gated on `fleet.view` and returned every approver's email, role, canonical `principal_ref` and subject-binding status — and `fleet.view` reaches every tenant role down to `viewer` AND sits inside the A20.3 machine ceiling, so an Operational Agent (and any viewer) could enumerate the tenant's APPROVERS. **`governance.view` is the 25th atomic permission**, READ ONLY, and independent of approval authority in BOTH directions: `operator` holds `action.approve` and does NOT get it (deciding a subject needs `/api/approvals/{id}/records`, which is evidence, not topology), while `auditor` gets it and still cannot approve. Granted to tenant_owner / site_admin / auditor; withheld from operator / viewer / platform_support; the machine ceiling is UNCHANGED and a test grants every binding at once then sweeps the vocabulary to prove the intersection excludes it. The group LIST moved with the DETAIL — it is the same category of fact with the individuals removed. **Three governance reads deliberately did NOT move** (`/api/policies/`, `/autonomy`, `/stop-switch`) on ratified grounds (A13/E0.3 and S1 D2: posture belongs to the people living under it), so nothing was replaced mechanically. `created_by` is authorship, not posture: `_policy_dict` became `{**_policy_posture_dict(p), "created_by": ...}` — additive, never serialize-then-strip. **Pre-merge remediation (A26.11), found by independent review:** the boundary demanded the right permission the WRONG WAY — `require_permission` and `has_permission(user, ...)` are NOMINAL role membership, and `get_scope`'s own docstring says a route guard cannot enforce a subset because `permission_subset` is PER GRANT. Reproduced against the running handlers under strict with persisted grants: a SITE-scoped `site_admin`, an ORG-scoped `site_admin` and a tenant grant whose subset was `["fleet.view"]` each read every approver's email and got `created_by`. **My own A26 test module could not see it** — its fixture overrode `get_scope` with a scope synthesised from the principal's permissions, so the test and the code made the same mistake and agreed; it was rewritten onto the persona matrix's production stack, and reverting the fix now turns 16 of its tests red. One predicate, `scope.permits("governance.view", tenant_object=True)`, asked by all three governance decisions (counted as AST calls, not text). `forbid_out_of_scope` was considered and REJECTED: `test_no_read_is_object_gated` holds that a GET narrows rather than 403s because a 403 confirms the object it refuses — and on `/groups/{id}` confirming the id exists IS the topology — so DENY takes the canonical read shape (no rows; 404), the same answer a cross-tenant id already gets. All three routes re-declared READ_SCOPED so the contract describes runtime truth in both directions. No migration, no backfill: a narrowed `permission_subset` deliberately does NOT acquire the permission because the role now carries it. Live on real Keycloak + PostgreSQL under BOTH postures (legacy_open: A23.10 synthesis for the never-granted intact, a real site grant still refused; strict: refused before granting, allowed after), plus 2 real-PostgreSQL tests for the timestamptz lifecycle and a hostile JSONB subset round-trip. Merged `6748baf`. 3974 → 3975 tests |
| 2026-09-07 | **A6-3 external-agent provenance + ingress operability shipped — Amendment A27** (D1–D5 + four implementation rulings ratified by Vinod; recorded in spec §9 + design §31 BEFORE the code). A6-1 gave an external runtime a governed way to WRITE and A6-2 a governed way to READ; neither built the HUMAN side, so HarkenIQ accepted governed work an operator could not attribute, diagnose or supervise. Two findings, one house pattern — **declared, written, and unreadable where somebody must act**: `origin` reached `admit_proposal()` and landed ONLY in audit-entry detail (no column, no payload) while `/api/approvals/` already spends the word `origin` on the queue LANE, so an approver saw "agent" both for a proposal HarkenIQ reasoned itself and for one an external runtime asked for; and `cc_agent_submissions` / `cc_agent_ingress_attempts` / `cc_agent_read_windows` were written by the submit route and the meter and read by NOTHING. **Provenance is its own concept (D2):** `cc_agent_proposals.provenance_type` (CC migration **0024**, additive, nullable), closed vocabulary `evaluator | external_ingress | unknown`, written transactionally inside `admit_proposal` so a proposal cannot exist without the provenance of its own creation — deliberately NOT the approvals `origin`, because one word answering two questions is how the surface came to answer neither. **No backfill (D3):** a pre-A6-3 proposal reads NULL and projects `unknown`, never manufactured into `evaluator`; an unrecognised stored value also reads `unknown`, so no future writer can put an unvetted string on a decision surface. **Ingress health (D4):** `GET /api/operational-agents/{id}/ingress` at `fleet.view`, READ_SCOPED, canonical resolver + `_require_visible_agent`, machine-self through the existing A25.5 helper — no new permission, A20.3 ceiling unmoved. **No invented connectivity (D5):** `last_authenticated_at` is the last PERSISTED authentication observation, `last_seen_source` is not exposed, and nothing says "connected". **Two HIGH findings from independent review, both fixed pre-merge.** (A27.13) A24.13 refuses an over-limit submission WITHOUT writing — correctly, twice over — but `build_ingress_health` then read `throttled` out of that same ledger, a key the attempt vocabulary does not contain, so the field was **zero for every agent forever** and A27.11's `throttled` state was unreachable by any amount of real traffic; rejections are now counted in `cc_agent_throttle_windows` (CC migration **0025**), one row per (tenant, agent, aligned minute) incremented in place, so **the bound is TIME not request count** — a million requests in a minute write one row — atomic by single-statement UPDATE plus unique-constraint-guarded insert inside a SAVEPOINT (A25.12), and only an ACTUAL rejection marks it because the request taking the last slot was SERVED. (A27.14) Provenance was on both human payloads and **the Console rendered neither**; a typed allow-listed module now applies the SERVER's reading rule so the two cannot disagree, on both intended surfaces — the approval-queue card where the decision is made, and the Operational Agent proposal history — with the queue's `origin` LANE untouched and a test whose only job is to fail if the two words are merged back together. Verified at the reviewed head: 4119 passed / 29 skipped, 7 real-PostgreSQL tests including 40 genuinely concurrent writers totalling exactly 40 in one row, Console tsc clean + build green + Vitest 41/41 (22 A27), and a compose gate GREEN on a wiped stack with six A6-3 steps — `real 429 x10 -> throttled=10, state=throttled, 1 throttle row(s)` and a real `0023 → 0025` upgrade on the LIVE database with 4 rows present that backfilled none of them. Independently re-reviewed at `1568d7b`; merged **`3e8369e`**, main verified. **Follow-up debt recorded, not implemented:** Console Vitest is not executed in CI (`ui-build` runs only `npm ci && npm run build`), the Operational Agent detail page couples four reads in one `Promise.all`, and the A23-2 gate step rewinds a destructive migration with Central Command running. 3872 → 4119 tests |
| 2026-09-07 | **PX0 product-experience architecture ratified — Amendment A28** (approved with amendments by Vinod after independent architecture review; docs-only, no production code). The first amendment to govern the SURFACE rather than a capability, because that surface is now the platform's weakest layer. **The finding:** HarkenIQ can state, for any action on any device, what the node can execute, what policy permits, whose scope reaches it, whether autonomy grants it, how many humans must approve, the blast radius, what happened and what the fleet learned — and the Console renders it as **37 pages with nine links between them**. Measured on `3e8369e`: **0** uses of `useSearchParams` (no incident, proposal, approval, wave or agent has a URL), 9 cross-surface links of the 16 the narrative needs, 519 inline `style={{}}` and 310 local `CSSProperties` with `detailRow`/`detailLabel`/`detailValue` declared 14× each, **0** `@media` queries in either UI, 53 `<label>` against 6 `htmlFor`, one `aria-live`, no focus trap anywhere, `/api/attention` rendered by a page titled "Risk & Exposure", and "External Agent" appearing **0** times though A6-1/2/3 shipped that actor class. **It is the house pattern at the product layer (A28.2):** `receipts.py` already composes the narrative as `submission_block`/`proposal_block`/`approval_block`/`execution_block`/`outcome_block`, assembled by `build_receipt()` — built in A6-2 FOR MACHINES, consumed by no human surface. The external runtime gets a governed lifecycle receipt; the operator gets a list. **PX creates no second domain model, RBAC system, scope resolver, capability authority, autonomy model, approval system, execution engine, identity model or agent governance plane**, and the node remains final execution authority. Disposition of the 44 assessed decisions: **36 ratified**, 3 ratified-and-re-sequenced (D-B1/B2/B3), 1 downgraded to a design constraint (D-IA3), **2 conditional** (D-AX3 pending PX5A's correlation proof — the earlier "almost no backend" claim was wrong and is withdrawn; D-SM3 pending an authoritative connectivity contract), 2 deferred (D-DS5 dark mode; D-HS2 warranty debt with NO owning slice), **0 rejected**, plus 6 new: **D-IA4** every Attention lens must resolve to an existing canonical contract or be DEFERRED — "fleet health" and "active automation" are not manufactured to fill a card (the D-AX3 rule, applied twice); **D-B4** the visual personality is operational precision, authority, safety, explainability and calm, readable at density for eight hours, and brand colour never competes with semantic severity; **D-B5** brand foundation lands at PX1B so PX2–PX7 are not designed against provisional aesthetics; **D-AC3** mobile approval keeps all eleven governance elements — no swipe-to-approve, no reduced path; **D-DS6** a domain component ships only with a real consuming workflow; **D-M2** unrelated behaviour and broad restyling never ride together, while accessibility and responsive work legitimately change both. **A6-4 is the next production slice and no PX implementation runs concurrently with it** (A28.5) — a new human read at `fleet.view` is automatically reachable by every machine principal, so the API-plane boundary is corrected first. WCAG 2.2 AA becomes a per-slice Definition of Done, not a PX8 cleanup. Program: A6-4 → PX1A → PX1B → PX2 → PX3A–D → PX4A/B → PX5A/B → PX6 → PX7 → PX8. Spec §9 A28 + design §32; no production code, permission, route or dependency changed |
| 2026-09-07 | **A6-4A External Agent API Plane shipped — Amendment A29** (boundary approved with amendments by Vinod; recorded in spec §9 + design §33 BEFORE the code). A6-1 gave an external runtime a governed way to WRITE, A6-2 to READ, A6-3 the human's ability to supervise both. None asked the prior question: **which of Central Command's routes an external runtime should reach at all.** Measured by executing the contract: a machine principal reached **46 of 98 declared routes** — not by any product decision, but because their required permission happened to be a member of `MACHINE_PRINCIPAL_CEILING`, and `fleet.view` alone opened 43 (campaigns, learning, outcomes, predictive risk, warranty, firmware exposure, approval-policy topology, autonomy budgets, tenant enforcement settings and its impact census, sites, Harken Node inventory). Exactly ONE was a write, so the write boundary was already correct and this is a read-surface correction. **The declaration already existed and was discarded:** `READ_CAPABILITIES` declares five machine read jobs and `INGRESS_CAPABILITIES` a sixth, each naming its own route family, and `auth.py` mapped them to coarse permissions and dropped the bindings before the guard could see them — so an agent bound to `attention` alone, the floor since `REQUIRED_READS` is `("attention","autonomy")`, read the whole estate and the A0 binding UI was not merely imprecise but untrue (the eleventh instance of the house pattern). **`MACHINE_SURFACE` is a POSITIVE declaration** — `(method, path) -> (surface, job)`, absent means HUMAN and not machine-reachable — with deliberately **no negative form**, because an exclusion list must be kept in step with every new route and fails OPEN when somebody forgets. Surfaces `HUMAN`/`MACHINE`/`BOTH`; **INTERNAL deferred on evidence** (zero internal routes exist and CC has no independently authenticated service-principal route class). Jobs are TYPED identifiers reusing the A0 binding vocabulary, never a parsed description string, with `self` the declared exception — a principal reading its own record is not a configured capability. **Result: 46 → 13, 33 removed, 3 MACHINE_ONLY, exactly 1 write; no new permission and `MACHINE_PRINCIPAL_CEILING` unchanged at `{fleet.view, incident.view, proposal.submit}`.** B2 honoured literally: fleet and learning lost direct route reach and their BINDING VOCABULARY is preserved and asserted intact. B8: governance conclusions yes, governance construction no — A26 kept posture at `fleet.view` reasoning about PEOPLE, and the ceiling had swept machines in as a side effect. **The invariant is the point, not the narrowing:** a non-vacuous test adds four permissions to the ceiling, proves the fixture non-vacuous by showing the OLD rule would have opened more, and asserts reach is unchanged. **Two independent-review findings, both closed pre-merge.** (A29.15 HIGH) `POST .../proposals` was declared MACHINE/job=proposals and its live path never asked — it reaches the handler through `Depends(get_current_user)`, so the guard consuming the declaration was not on its path; removing the declaration changed nothing. It cannot be a route dependency (dependencies resolve BEFORE the handler, so the refusal would precede `admit_attempt` and be free — the hole A24.13 closed), so the DECISION was split from the refusal HANDLER: `evaluate_route_surface()` is pure and shared, `enforce_route_surface()` is the read plane's handler, and the write consumes the same decision inside A24's attempt ledger. Proven by RUNTIME: declaration removed → 403, declared job changed → 403 with permissions untouched, restored → 201, and the refusal charged. (A29.16 MEDIUM) refusals were charged but not attributable — `/metrics` is unauthenticated so a tenant or agent id may never be a label there (A25.11), which is exactly the attribution an operator needs. The evidence now rides the window row that already exists (CC migration **0026**): `surface_refused`, `last_surface_refused_at`, and one counter per CLOSED reason, bounded by (tenant, agent, window) and never by request volume. **The defect the remediation itself introduced and the review caught:** `_charge_machine_read` declared `-> datetime`, computed the window and **never returned it**, so the recorder's fallback recomputed and the minute-boundary race stayed open behind a commit message claiming it was closed — the twelfth instance of the house pattern, authored by the fix for the eleventh. The window is now computed ONCE in `admit_read`, returned, and required by the recorder with no fallback; a real-chain test forces a boundary between charge and record. Also found there: `except Exception` swallowed a `NameError` at DEBUG so the evidence silently stopped being written (now WARNING), and the evidence had no production reader — `surface_refusals` is projected through the EXISTING A6-3 `/ingress` contract, same `fleet.view`, same READ_SCOPED, same machine-self rule, no new endpoint. Live on a wiped stack: 19 off-plane routes refused a real machine token, on-plane reads and every human read unaffected, `self` readable with no binding, 5 refusals charged 5 reads in 1 row, `operator reads 24 surface refusal(s)` and the machine reads its own. Reviewed head `34a0039`, merged **`71a9165`**. 4119 → 4171 tests |
| 2026-09-13 | **A6-4B0a Grant lifecycle integrity shipped — Amendment A30** (boundary + D1/D2 ratified by Vinod; recorded in spec §9 and design §34 BEFORE the code; A6-4B was re-sliced into five by A30.9 and this is the first, a security correction and not product capability work). Objective: **expired means expired everywhere operational reach is evaluated.** `OperationalAgentRepo.list_scopes()` filtered `revoked_at` and NOT `expires_at`, and it was the input every operational reach path resolved from — so an EXPIRED `site`, `device` or `device_class` grant still yielded devices to the evaluator, the A6-1 ingress re-derivation, dry-run, runtime, agent-view and preflight, four of them machine-reachable. Revoked was correctly closed; expired was fail-OPEN. A3 identity is independent of scope grants, so an agent whose grant had lapsed still held a valid credential and went on proposing. **NOT one missing WHERE clause:** Central Command had TWO reach paths — a canonical one that is lifecycle-correct but whose `site_ids` projection cannot represent a `device` or `device_class` grant, and a raw one that is type-complete and lifecycle-blind — and `agent_runtime` passed BOTH, so the runtime compensated for the first defect by importing the second and took the union of their errors. The two questions are separated by NAME now: `list_administrative_scope_rows()` is what is CONFIGURED, expired rows included, because `clear_scopes` must see a lapsed row to retire it; `load_agent_reach()` is the one operational answer, and administrative raw-row lifecycle access is deliberately NOT an authorization source. **Pre-merge remediation (independent review, HIGH — A30.17):** a defect predating B0a that B0a could not ship around — CC had TWO places deciding whether an approved proposal may cross CC→SM and they asked different questions (the synchronous approval path asked identity, activation, tenant, stop switch and pause and NEVER reach or the capability binding; the background pass asked reach and binding and never the stop switch). Reproduced by execution before any change: on the synchronous path an ACTIVE, an EXPIRED and a REVOKED grant all returned HTTP 200, called the Site Manager and dispatched; a dual approval begun under live authority and completed after expiry dispatched; the background path dispatched past an asserted stop switch. **One gate:** `agent_runtime.revalidate_dispatch()` is the one CC dispatch decision, BOTH paths call it and nothing else assembles the inputs, and it is the only caller of the fail-closed `dispatch_permitted` algebra, whose `DISPATCH_GATES` gain `effective_scope` and `capability_binding` and where an unsupplied gate is a refusal, not a pass. Approval is HISTORICAL governance evidence; execution permission is CURRENT authority, read in the same transaction immediately before the Site Manager call — and a lifecycle refusal writes only the dispatch reason, never erasing the human who approved. **Second independent re-review — the revocation half of the live proof (A30.21):** expiry was proven live and a revoked machine IDENTITY was proven to refuse SUBMISSION, but nothing at the production boundary had revoked a SCOPE GRANT underneath already-approved work. Compose gate steps AI–AL now do, on both paths, with a fresh agent that owns its state and TWO site grants — G_T on the target's site and G_O on another device-bearing site that is never touched, so the agent keeps real reach and a refusal has to be about the TARGET. Synchronous: approval 1 of 2 recorded while authorized, G_T revoked through `DELETE /api/scope-grants/{id}`, approval 2 completes → refused on `effective_scope`, ZERO SM directives, both approvals still on the E0.1 ledger. Background: approved while authorized with the site briefly unreachable — the case the approval route deliberately leaves `approved` for the background pass — G_T revoked, the real operational-agent loop withholds the still-approved proposal, ZERO SM directives; restoring ONLY G_T then dispatches the same approved proposal, which is the causal control. Attribution follows `DISPATCH_GATES` order, the sixth gate's message meaning the first five passed, and each refusal names WHICH lifecycle fact emptied the reach because revoked, expired and inert-vanished-target all share one message. **Two defects that proof found in ITSELF:** it borrowed `gate-a23-admin2` as first approver, and A23-3 has two administrators revoke each other CONCURRENTLY while asserting only that exactly one wins — restoring the owner only when the owner loses — so that identity ends revoked about half the time and the step passed on a coin flip; a step's identities are part of the state it must own, and it creates and grants its own now. And the causal control waited on `status='dispatched'`, a status the settle pass moves to `completed` (which can never match) while `wait_for` samples every 5s, so a fast settle would have timed the control out AFTER it succeeded; it waits on the directive id, which once written is never cleared. **A consequence recorded rather than hidden (A30.18):** `device` and `device_class` work now dispatches, confirmed by review as correct target-aware authorization and not accidental B0b work — reach is resolved over the proposal's ACTUAL target, so an unrelated surviving grant cannot authorize another target. **F1 deliberately remains OPEN:** `ResolvedScope.site_ids` stays lossy by construction and is NOT compensated for with raw grant rows, pinned by an executable test that names A6-4B0b as its owner. No new route, permission, migration or ceiling change; CC head stays `0026`; `MACHINE_PRINCIPAL_CEILING` unchanged at `{fleet.view, incident.view, proposal.submit}`. Independently reviewed twice and remediated twice; final verdict MERGE at reviewed head `8af6d4b`, merged **`20d90ba`**, main verified with merged-main CI green including the compose gate. Stated limits rather than implied ones: the dispatch-side identity gate has no live coverage, and the revocation proof exercises `site`-type grants only. B0b, B0c, B1 and B2 not started. 4171 → 4240 tests |
| 2026-09-15 | **A6-4B0b-S1 Multi-target authorization integrity shipped — Amendments A30.22 + A30.23** — **IMPLEMENTED · MERGED · MAIN VERIFIED.** PR #43 (`a6-4b0b-s1-multi-target-auth-integrity`), independently reviewed twice: first review of `494432b` returned ONE HIGH, remediated as `0d1fd26` (docs-first `e87bc77`), second review MERGE; merged **`f6fc654`**, merged-main CI green (Tests: compose-gate, 3.11, 3.12, ui-build; publish-agent) and the S1 invariants re-run on merged main. Cut out of general B0b and landed first and alone because B0b would have made device- and class-scoped principals REAL and therefore discoverable. **What the Phase-1 inventory found:** an authorization DECISION over a SET evaluated against ONE member — `_decide_campaign_wave` handed `device_agent_ids[0]` to the approval gate, so a principal holding a `device` grant on A approved a site-wave over [A, B, C], the governing policy followed the first device's class and the ledger named one device; three more of the same family and nothing else in 89 classified paths: the runner dispatched an APPROVED wave re-checking stop switch, status, plan hash and capability and never the approvers' CURRENT authority over the set; `install_bound_skills` fanned out over the device list the PREFLIGHT stored; and the wave subject digest was a lookup key never re-verified. **One rule (A30.22):** AUTHORIZED means EVERY target is covered by the decider's CURRENT canonical scope — never any, never the first, never the site as a proxy, never a visible subset; the set is never narrowed to fit the caller (capability may still narrow what EXECUTES, A18 D2 unchanged). `harkeniq_cc/target_authority.py` holds it: every target through the canonical `permits(..., device_agent_id, device_class)` with the class read from the CURRENT fleet row (a `device_class` grant may sign a uniform wave and refuses a mixed one; an empty class never matches, `Grant.covers_device`'s rule); refuse on empty / duplicate / unknown-at-this-site / digest mismatch; policy resolved per DISTINCT class and DISTINCT policies refuse rather than inventing multi-policy arithmetic; the ledger names the whole set; at dispatch, after the plan-hash check and before capability narrows, each ledger approver is re-resolved token-lessly through the ONE loader and E0.1's completion rule is re-run — failing that the wave is WITHHELD (`revalidation = authority_lost`, status and ledger untouched, one audit entry per distinct cause, dispatched on a later pass if authority returns); skill delivery re-resolves `load_agent_reach` and INTERSECTS the stored preflight list, narrow-only, every removed device recorded as skipped. **The HIGH the first review found, and A30.23:** the dispatch-time basis was `ROLE_PERMISSIONS[authority_snapshot.role]` — the role the token carried WHEN THEY DECIDED — applied to the grants held now, stated in §34c as a limit; an operator demoted in Keycloak after approving, grants intact, still counted toward dispatch. A principal's authority has two halves and A30.17 admits no half. Now `auth.role_basis` is the ONE role rule (`pick_role` → `ROLE_PERMISSIONS`), called by `get_current_user` AND by `target_authority.approver_current_basis` (pinned structurally, both directions); the CURRENT effective realm roles come from Keycloak through the Console over the EXISTING CC→Console shared-key internal channel, which gains one READ (`GET /api/internal/tenants/by-realm/{realm}/principals/{subject}/authority`: found / enabled / composite realm roles) — an identity-plane read, not a tenant-plane route, so `ROUTE_CONTRACT` and `MACHINE_SURFACE` are unchanged and CC still holds no Keycloak admin credential (A20); `authority_source` is a REQUIRED input with no default; FAIL CLOSED with the cause named on the withheld audit entry from a closed set (`current_role`, `scope`, `principal_not_found`, `principal_disabled`, `unresolvable`) so a demotion is distinguishable from a revocation and from an outage; the ledger is read and never written and `target_authority.py` contains no `authority_snapshot` read at all; a restored role lets the SAME historical approval count again — re-established, not re-made. **Verified invariants, final:** all-target authorization (refused-not-recorded for [A] and [A,B], recorded for [A,B,C], one-variable control); immutable wave/plan integrity (tampered device list and plan hash refused, even for the owner); device-class fail-closed (empty class never a wildcard, unknown class matches no grant); missing-target and duplicate refusal; tenant isolation; per-class policy selection with conflicts refused; current-role demotion blocking dispatch (0 dispatch rows, 0 SM directives); historical approval preservation (recorded role and ledger rows asserted unchanged after every withhold; the snapshot provably not consulted in EITHER direction); restore-authority positive control (same approval, same ledger rows, three dispatches); permissions (25), `MACHINE_PRINCIPAL_CEILING` `{fleet.view, incident.view, proposal.submit}`, `MACHINE_SURFACE` (13), the `server \| switch` vocabulary and every migration head (CC `0026`, Console `0004`, SM `0010`) byte-identical to pre-merge main. Proven on real PostgreSQL (JSONB ledger, timestamptz expiry) and live in the compose gate (AM–AR): three declared devices in one wave at site B, grant on C revoked through the production route → withheld → restored → dispatched; then a REAL Keycloak realm role-mapping DELETE after a recorded approval, the Console read reporting it, a fresh token for the demoted user 403 on approve (the request path and the dispatch gate share one rule), the completed decision withheld with `current_role` on the audit entry, recorded role still `operator`, 3 live grants, then the mapping POSTed back and the same two ledger rows dispatching three. **S2 (device-identity widths): NOT REQUIRED** — real ids are 16 hex, every compared operand is wider and written verbatim, PostgreSQL raises on overlength. **Non-blocking follow-ups, recorded exactly as they stand:** no lock between the current-authority read and the CC→SM call (A30.22/A30.23 both state it); the fail-closed cases beyond demotion are proven through the real Console router at unit level, not in the live gate; one Console→Keycloak round trip per approver per approved-wave pass, uncached by design; `pick_role`'s `viewer` default kept identical to the request path (carries no `action.approve`); the SM's legacy R4-3 firmware surface does not enforce the E1.3 one-site boundary on its create and skills-install lists; `DispatchAction` supplies `agent_scope=True` as a constant; `InstallSkill` keeps a pre-E0.2 unresolved-site fallback; `_scope_rule_within` never resolves a `device` rule's site (over-restrictive, never fail-open); the identity-width invariant guards no device column; and the pre-existing intermittent SM `test_directives.py` teardown hang reproduced ONCE locally during this slice with its first captured stack (asyncio runner close waiting on a task that never finishes cancelling) — unrelated to the diff, re-run and every CI run green. F1 (`ResolvedScope.site_ids` lossy) stays deliberately OPEN and is general B0b's. B0b general, B0c, B1, B2 and the datacenter taxonomy NOT started. 4240 → 4338 tests |
| 2026-09-19 | **A6-4B0b-S2 Permission-aware read reach shipped — Amendment A30.24** — **IMPLEMENTED · MERGED · MAIN VERIFIED.** PR #45 (`a6-4b0b-s2-permission-aware-read-reach`), docs-first `ceed789` then code `7be2df5`; independently reviewed by Codex at the immutable head `7684b73` — **verdict MERGE**; merged **`3babb30`** (the merge's second parent IS the reviewed SHA and the merged tree is identical to the reviewed tree), merged-main CI green (Tests: compose-gate, 3.11, 3.12, ui-build; publish-agent) and the S2 invariants re-run on merged main. A security prerequisite cut out of general B0b and landed first and alone, for the reason S1 was: producing the general B0b boundary reproduced a second defect UNDER F1 that points the other way, and B0b would have extended it to two more dimensions. **P1 — fail-OPEN, pre-existing since E1.2, now CLOSED.** `_project` builds `ResolvedScope.site_ids`, `tenant_wide` and the permission-less `covers_site` / `covers_device` / `covers_org_unit` from EVERY effective grant whatever that grant carries, and a read route takes its PERMISSION from the role (`has_permission` — nominal, as A26.11 already established): with grant A over site-a carrying the full role and grant B over site-b narrowed to `incident.view`, `site_ids` was both sites while `permits("fleet.view", site_id="site-b")` was False, and site-b's fleet, approvals, outcomes, audit, campaigns and sites answered. Two stronger forms, both reproduced: the `tenant_wide` shortcut (a TENANT grant narrowed to one permission unfiltered every read in the platform), and — the realistic face — no explicit subset at all: A23-3 made the role a grant RECORDS a ceiling, so "`site_admin` at site A, `viewer` at site B" under one `site_admin` token read site B's approval queue and grant list. It is the defect A26.11 closed for `governance.view`, left standing on every other read: the right rule already existed in `permits()` and the readers did not ask it. **The invariant, LOCKED:** `READABLE(P, r) = ∃ effective grant G: P ∈ G.permissions ∧ G covers r` — permission from one grant never combines with reach from another, role membership never substitutes for what a grant carries, nothing is synthesized. **The primitive.** `scope.read_reach(scope, *permissions)` returns a frozen `ReadReach`: `_project`'s arithmetic over ONLY the non-inert grants that carry one of the named permissions (`"*"` counts, exactly as in `permits`), its `covers_*` delegating to those grants' own `covers_*` — so it is `permits()` PROJECTED, not a second resolver, and a generated matrix asserts the equality for every permission × 11 grant mixes × every site, org unit, device and tenant target. Several permissions mean ANY OF, for `require_any_permission` routes; contextual ancestry is not an input. `scope.where_reach` is its mirror and only other constructor — the one legitimate permission-neutral filter, for the WHERE-only scope (A22.13) the evaluator, dry-run and ingress re-derivation narrow by, with exactly three call sites — and the two PARTITION the scopes: each raises on what the other accepts. **`ResolvedScope.site_ids` is deliberately UNCHANGED** and stays permission-neutral (an agent's operational reach, `/api/scope-grants/me` and the L2 approval snapshot still ask it honestly); what changed is that it can no longer be a read FILTER: `scope_sites`, `_audit_scoped` and `CampaignRepo.list_all` accept `None` or a `ReadReach` and RAISE on anything else, and a source-level guard fails the suite when an `api/` module reads the neutral projection off the resolved scope (two named self-description sites allow-listed) or filters on a permission its own route guard does not accept. All 35 call sites derive their reach from their route's OWN guard: `fleet.view` (fleet, agents, capabilities, firmware exposure, predictive, warranty, attention, outcomes, patterns, learning, autonomy narrowing, campaigns, Operational Agent reads, sites), `incident.view`, `action.approve` or `audit.view` (approval queue, history, records), `audit.view`, `site.view` (org tree), `user.view` or `audit.view` (grant list), `site.manage` (campaign preflight caller-intersection); handlers keep `scope=Depends(get_scope)` so the A23-1 consumption census and `ROUTE_CONTRACT` are untouched, and the 404-before-403 visibility that precedes a mutation now asks the object's READ permission. **What a human can see change:** only a principal holding a grant that withholds the permission a route requires reads less there — full-role tenant, org and site personas read through sets EQUAL to the old projection for every permission their role holds, asserted as arithmetic. **F1 is deliberately still OPEN** and asserted open: `ReadReach` carries `device_ids` / `device_classes`, already permission-narrowed, and no repository filter consumes them — general B0b builds its table-aware device predicates ON this primitive, which is why S2 had to land first. **Proof:** the generated algebra; behavioural proofs on the production stack with persisted grants under STRICT, every narrowing paired with an un-narrowed CONTROL; mutation-checked (making `_reach` permission-blind turns 29 of 83 red); real PostgreSQL (JSONB subset, timestamptz expiry, foreign-tenant grant, paginated totals agree with rows); compose gate steps AS–AU on a wiped stack with real Keycloak (the control reads both sites everywhere; the narrowed site grant reads site B under `incident.view` and nothing else; a narrowed TENANT grant reads no fleet, site or audit). **Delivery note:** a pre-existing wall-clock time bomb in `tests/unit/test_checkpoint.py` (a hard-coded "fresh" baseline crossed the agent's 30-day staleness rule on 2026-09-18) turned `test (3.11)` / `test (3.12)` red on every branch; it was fixed TEST-ONLY in its own PR #46 (`4c5e964` → main `bc6fcb6`: fixtures generated relative to now, the 30-day rule pinned from both sides rather than weakened) and NOT folded into the security PR, and S2 was brought up to that main by MERGE, not rebase, so the two S2 commits were carried byte for byte and the PR diff Codex reviewed is byte-identical to the one first pushed. **Unchanged:** the permission vocabulary (25), `ROLE_PERMISSIONS`, `MACHINE_PRINCIPAL_CEILING` `{fleet.view, incident.view, proposal.submit}`, `MACHINE_SURFACE` (13), `ROUTE_CONTRACT` (98; zero routes added), every response shape, every migration head (CC `0026`, Console `0004`, SM `0010`), `resolve()`, `permits()`, every mutation gate, S1, and the `device_class` vocabulary `server \| switch` — the datacenter taxonomy is not started. **Recorded follow-ups, none a blocker:** `/api/scope-grants/me` still publishes the permission-NEUTRAL `site_ids` (by decision — it is a self-description), so it over-predicts what a mixed-grant principal can read and a client must not use it to predict reads; COMPOSITE PROJECTIONS — a route guarded by one permission that embeds data whose own read is guarded by another — follow the ROUTE's permission and were left as product behaviour: incident detail's pending-approvals block under `incident.view`, and fleet summary's `incidents_open` and the attention contract's incident-derived state under `fleet.view`; **D10 is NOT a prerequisite**: the Site Manager's outcome snapshot falls back to its internal `devices.id` (a 32-hex uuid, not the canonical 16-hex node id) only if a device row vanishes mid-request, which no SM code path can cause (the rows come through an INNER JOIN on `devices.id`, and the only `session.delete` removes a fault-domain membership) and which would fail CLOSED for device principals — a skip-and-warn hardening is recorded for general B0b's outcome-ownership work, not done; the PostgreSQL integration proofs are DSN-gated and not executed in CI (as S1's); `tests/unit/cc/test_warranty.py` hard-codes an `endDate` of 2027-01-01, the same class of bomb. General B0b, B0c, B1, B2 and the taxonomy NOT started. 4338 → 4421 tests |
| 2026-09-21 | **A6-4B0b-S3 Autonomy scope isolation shipped — Amendment A30.26** — **IMPLEMENTED · MERGED · MAIN VERIFIED.** PR #49 (`a6-4b0b-s3-autonomy-scope-isolation`), docs-first three times (`f92fcd5` A30.26 + design §34g → `32a49cc` a site-derived autonomy fact is a `fleet.view` fact on every projection → `b3705cc` a stored reason follows the row it was copied from) then code `1d4250a`; independently security-reviewed by Codex at the immutable head `1d4250a` on base `80469ab` — **FINAL VERDICT MERGE**, no BLOCKER / HIGH / MEDIUM / LOW; merged **`ee0803b`** with `--match-head-commit` (the merge's second parent IS the reviewed SHA and the merged tree is identical to the reviewed tree), merged-main CI green (Tests: compose-gate incl. BB–BF, 3.11, 3.12 at 4565 passed / 69 skipped, ui-build; publish-agent) and the S3 + S2 + S1 invariants re-run on merged main (313 unit; 25 real-PostgreSQL = S3 17 + S2 4 + S1 4, CC head `0026`). A security prerequisite cut out of general B0b and landed first and alone, as S1 and S2 were: B0b's LIVE gate found **P2**, pre-existing since A23-1 — `narrow_to_sites` dropped list items that carry a `site_id`, and an aggregate has none. **P2 had three faces, all reproduced on main.** (1) AGGREGATES: a site-A reader's `/api/autonomy/` and Operational Agent view carried every site's error-budget totals, `sites_dropped_back`, suppressed fault-domain names, the per-site budget map, evidence, advancement, `reported`, `sites_reporting_active` — and the DISPOSITION folded from them. (2) A PROBE ORACLE: the composer applied the `site_id` query parameter BEFORE narrowing, so `?site_id=<hidden site>` returned that site in isolation (live on main: `scope.sites: []` beside `error_budget total 470047`). (3) PERSISTED COPIES: the evaluator stores the tenant-wide class row's `blocking_conditions` and learned signals on every proposal, returned as stored by seven projections (approval queue, both decision responses, agent detail, proposal list, dry-run, the machine submit response and machine receipts) — five of them machine-reachable. **The invariant, LOCKED:** for any autonomy projection returned to principal P, EVERY site-derived fact is derived only from sites covered by P's current canonical effective scope. **SELECT, then aggregate:** `autonomy.select_site_inputs()` generalises the composer's old single-`site_id` pre-filter to the reader's SET and runs first; the fold is unchanged and only ever sees what was selected, so every aggregate, boolean, disposition, reason and advancement distance is right at once, the probe closes (`site_id` narrows WITHIN the selection — a site outside reach answers byte-for-byte as a site that does not exist), and an empty reach is empty by construction (`reported` false — S5: unreported is UNKNOWN, never safe — nothing zero-filled, tenant-owned posture preserved). `narrow_to_sites` is **DELETED, not repaired**: a function whose input is a composed contract cannot satisfy the rule. **No new authority:** the reach is S2's `read_reach(scope, "fleet.view")`; `load_autonomy_contract(reach=...)` is a REQUIRED keyword — a `ReadReach` for a principal, or `None` for the five INTERNAL decision paths (evaluator, ingress re-derivation, dry-run reasoning, campaign submit, activation preflight), allow-listed by call site in an AST test, so execution semantics are byte-identical. **A stored verdict is narrowed where it is read**, behind a frozen `AutonomyView` every proposal projection REQUIRES (raises on `None`, a set or a bare scope): a blocking row is kept when tenant-scoped or naming a held site and dropped otherwise (fail closed); a site-scoped learned signal follows its site; a stored reason that is the text of a withheld row and of no kept row becomes one neutral sentence (`WITHHELD_REASON`). The view is deliberately `fleet.view` reach and NOT the route's own: an approver can hold `action.approve` at a site whose grant withholds `fleet.view`. No write or decision path changed; historical rows are covered. **Proof:** deletion equivalence as the master oracle (a scoped reader of the full estate == a tenant-wide reader of an estate where the hidden sites do not exist — all 16 site subsets pure, 12 personas over HTTP under STRICT on two separately seeded stacks); poisoned hidden rows proving filter-BEFORE-aggregate by behaviour with a tenant-wide control that raises; a sentinel estate where magnitude encodes provenance and every key and leaf is walked; 24/24 deliberate regressions killed; main-vs-branch differential — tenant-wide reader byte-identical over 24 compositions, scoped readers 57–147 hidden-site leaves on main → 0; 17 real-PostgreSQL tests; live gate BB–BF on a wiped stack with real Keycloak, the error budget seeded AT THE SITE MANAGER and carried by the real poller. **Unchanged:** permissions (25), `ROLE_PERMISSIONS`, `MACHINE_PRINCIPAL_CEILING` `{fleet.view, incident.view, proposal.submit}`, `MACHINE_SURFACE` (13), `ROUTE_CONTRACT` (0 routes added), response shapes, every migration head (CC `0026`, Console `0004`, SM `0010`), `resolve()`, `permits()`, `read_reach`, S1, S2, the Console. Only org-, site-, device- and class-scoped readers change, and only by reading LESS. **Reported, NOT changed — recorded as follow-ups by the independent review and pinned by tests to invert when ruled:** **E1** the evaluator / preflight / campaign-submit tenant-wide disposition fold is execution semantics from S5/E0.2 (a hidden site's drop-back or spent budget can still route a site-local proposal to a human; the condition and its location are withheld) — changing it could WIDEN autonomy, so it needs its own architecture ratification and is NOT an S3 follow-up implementation; **E2** tenant-wide outcome statistics frozen on a proposal (`evidence.outcome_evidence` and the `_rationale` sentence — a scoped reader reads 31 executions, not its local 7); **E3** cohort learned signals are tenant knowledge under A23. Also noted: an agent's configured scope rules name every site it is bound to, to any reader who holds one of them (A23-1 agent visibility, outside this invariant). General B0b (PR #48, frozen at `19f65a6`), B0c, B1, B2 and the datacenter taxonomy untouched. 4421 → 4565 tests |
| 2026-09-22 | **A6-4B0b-S4 Learned-signal payload isolation shipped — Amendments A30.28 + A30.29** — **IMPLEMENTED · MERGED · MAIN VERIFIED.** PR #51 (`a6-4b0b-s4-learned-signal-payload-isolation`), docs-first every time (`675cce7` A30.27 recorded-not-implemented + A30.28 + design §34i, amended `a1a4436`/`96d2efe` before code `540a0cd`; `921d420` A30.29 + design §34j before remediation code `8e70272`; the A30.29 atomicity paragraph in `632b443` beside its code). Independently security-reviewed by Codex THREE times: `540a0cd` FIX BEFORE MERGE (one HIGH), `ded979d` FIX BEFORE MERGE (a second HIGH), and the immutable head **`7057cdc`** on base `a5f9549` — **FINAL VERDICT MERGE**, previous HIGH CLOSED, second HIGH CLOSED, every named invariant PASS, S1/S2/S3 PASS; merged **`d01d74c`** at 2026-09-22T12:09:21Z with `--match-head-commit` (the merge's second parent IS the reviewed SHA and the merged tree `93b8058` is identical to the reviewed tree), merged-main CI green (Tests: compose-gate GREEN incl. BG–BO, 3.11 and 3.12 at 4719 passed / 82 skipped, ui-build; publish-agent) and the S4 + S3 + S2 + S1 invariants re-run on merged main (496 unit across the five A30 modules + migration chains; 38 real-PostgreSQL = S1 4 + S2 4 + S3 17 + S4 13 incl. both live upgrades; Console Vitest 47/47). A security prerequisite cut out of general B0b (R7 of the ratified S3-E1/E2/E3 package) and landed alone, as S1–S3 were. **E3-F1 CLOSED, A23 retained:** a vendor/model cohort CONCLUSION is tenant knowledge and every reader keeps it; the defect was the PAYLOAD under it — readers selected rows by scope and returned CONTENT as stored, so a site-A principal entitled to the cohort row read a hidden site's id and failure count, `sites_affected`, the tenant totals and a sentence restating them, through `/api/learning/signals`, `/api/attention/` (**on MACHINE_SURFACE**), incident `prior_learning`, `/api/autonomy` `learning[]`, `/api/outcomes/patterns`, the frozen `learned_signals` on all seven proposal projections, `evidence_cited`, `/api/learning/cycles` and the CC→SM distribution payload. A23-1's sweep and S3's sentinels missed it because every seeded signal had `evidence = {}`. **One projection** (`harkeniq_cc/learning_projection.py`, pure) reached only through `governance.LearningView` = S2's `read_reach(scope, "fleet.view")`: a tenant-wide reader gets what is stored BY IDENTITY; anyone else gets bounded bands (rate to 5%, confidence to a 0.25 grid — exact confidence is a count in disguise — every count withheld and NAMED in a `withheld` list, site maps cut to the reader's own sites and always marked `partial`), and text follows its evidence — re-rendered from the projected evidence by the generator that wrote it, or reduced through that generator's grammar and replaced whole if count-shaped residue remains; an unknown evidence key is withheld. Distribution: a receiving site is a reader holding exactly itself. `load_attention(learning=)` REQUIRED, `None` allow-listed by call site for the three internal decision paths; `_narrow_sites` DELETED. `/api/learning/cycles` re-declared `UNSCOPED → READ_SCOPED` (a count of hidden sites is exactly what a scoped reader may not be handed). **Second finding from the slice's own twin estates:** `get_metrics()` sorted cohorts by TENANT attempt total, so detection order — the pattern list, the cycle list, sub-second learning instants, `?limit=` cut in SQL and the order a Site Manager is pushed and cites patterns in — ranked cohorts by a hidden total; scoped readers are ordered by what they are SHOWN, learning instants floored to the minute, `limit` applied after projection. **Codex HIGH one (A30.29, Option A ratified by Vinod + fix the multi-site SM store):** A30.28 bounded a pattern CITATION and the Site Manager's LLM diagnosis and the candidate YAML generated from it were written from a prompt carrying whichever pattern payload the SM held — the whole tenant's before A30.28, possibly ANOTHER site's on a multi-site SM — and CC returned `generated.*` and `yaml_text` verbatim (reproduced: a site-A principal read "across 3 sites (35/54)" at 200). **Generation provenance** is now ONE marker `generation_visibility` ({scope site\|tenant, site_id, projection_version}) defined once in `harkeniq.generation_provenance` for both services, naming the authorization boundary of the evidence projection an artifact was generated FROM, never the evidence: the SM stores pushed patterns per `(site_id, pattern_id)` in `sm_site_fleet_patterns` (SM migration **0011**; PushPolicy resolves the receiving site by CC's id, refuses an unresolved site and a payload marked for another site; legacy `sm_fleet_patterns` read at boot as unmarked evidence only where a site holds no row of its own — never written, never dropped), `_enrich_verdict` consumes the device's OWN site's projection and writes the combined marker INSIDE the explanation in the same assignment as the text and beside the candidate's YAML (`combine`: unknown and `tenant` are contagious, two sites is `tenant`; `sm_candidate_skills.site_id`, declared by E1.3 with no writer, is written too); proto `CandidateSkill.generation_visibility_json` (tag 9, additive). CC (`cc_candidate_skills.generation_visibility`, CC migration **0027**, NO backfill anywhere) projects `generated.*` and `yaml_text` through the reader's `LearningView`: a tenant-wide reader gets the stored fields, a scoped reader only when the marker names a site they hold NOW; missing, malformed, `tenant` or another site → the whole block WITHHELD, built from constants so a field a future provider adds cannot survive. **Provenance is evidence, not authority:** revoke → withheld, restore → the same stored artifact, unchanged. Console renders the server's withheld flag (`generatedContent.ts`). **Codex HIGH two (`632b443` + `7057cdc`): content and provenance were not updated ATOMICALLY** — a same-`skill_id` replay could replace `yaml_text` while RETAINING the earlier site-A marker, so stale provenance acted as an authorization credential. Now `CandidateSkillRepo.upsert` treats generated content and its provenance as ONE security unit: same-id writers serialized on a transaction-scoped advisory lock acquired BEFORE the row read (which also serializes the first insert, where `SELECT FOR UPDATE` has no row); a changed `yaml_text` or `warnings` with missing/malformed/invalid incoming provenance stores UNKNOWN (`NULL`) so every scoped projection fails closed; a byte-identical replay may keep the marker already attached to those exact bytes (identity alone never proves content equivalence); and the poller upserts a snapshot's candidates in canonical `skill_id` order so two concurrent pollers cannot form an A→B / B→A deadlock. **Proof:** twin estates written by the REAL `IntelligenceEngine` differing only at a hidden site and sharing every band — byte-equal on every surface for 11 scoped personas and a machine principal, different on every surface for the tenant owner; 22/22 + 14/14 real mutants killed; the real IngestService with a prompt-echoing provider on one SM serving two sites; the historical attack for site / device / class / machine readers with the owner as control; real PostgreSQL (JSONB marker, composite key, both upgrades with rows present, atomic replay + concurrent cross-site ingestion); compose gate GREEN on a wiped stack with steps **BG–BO** (real engine loop storing E3-F1, owner exact vs site-A bounded, attention + incident still learn, per-site marked rows on ONE Site Manager, historical attack incl. a machine token, revoke → withheld → restore, live CC `0026→0027` + SM `0010→0011` upgrades with rows backfilling none). **Unchanged, verified on merged main:** permissions (25), `ROLE_PERMISSIONS`, `MACHINE_PRINCIPAL_CEILING` `{fleet.view, incident.view, proposal.submit}`, `MACHINE_SURFACE` (13), `ROUTE_CONTRACT` (98; 0 routes added), `auth.py` / `scope.py` / `machine_identity.py` untouched, `resolve()`, `permits()`, `read_reach`, every decision path, S1, S2, S3, Console `0004`; general B0b / PR #48 (`19f65a6`) untouched; E1/E2 NOT implemented; taxonomy, B0c, B1, B2 NOT started. Stated limit: the Site Manager's own site-token API returns its rows as stored (E1.3's site-authority plane). **Follow-ups, all NON-BLOCKING:** candidate identity strengthening (`skill_id` alone never proves content equivalence — a content digest would); the fixed pre-projection SQL windows (patterns 1000 / signals 500 / cycles 200 — a membership side channel above the cap); minute-boundary timestamp residue (a detector pass straddling a minute, ~1 in 10⁴); `observation_count` semantics at minute grain; predictive `cohort_failure_rate` / `outcomes_considered` (S3-E2's family); the live gate does not exercise a machine token on `/api/attention/` (unit-proven only); the intermittent `tests/unit/sm/test_directives.py` teardown hang (infrastructure, root cause open); Console Vitest and the DSN-gated PostgreSQL proofs are not executed in CI (PX1A owns the first). 4565 → 4719 tests |
| 2026-09-22 | **A6-4B0b Canonical reach convergence shipped — Amendment A30.25** — **IMPLEMENTED · MERGED · MAIN VERIFIED.** PR #48 (`a6-4b0b-canonical-reach-convergence`), docs-first (`916bb44` A30.25 + design §34f, then `a76d411` recording what the live gate found) before code `19f65a6` on the pre-S3 base `80469ab`; brought up to main `d73ec81` by MERGE, not rebase (`14fed99`, **S3 and S4 winning every overlap** — the local `narrow_to_sites`/`holds_no_site` edit dropped, the frozen pre-S3 autonomy oracle retired, S4's `LearningView`/generation-provenance readers taken as given), docs reconciliation `0cb96a7` BEFORE test reconciliation `aa8eb4c`. Independently reviewed at the immutable head **`aa8eb4cffefe1054c5d30691c19eb49ee5d191d2`**: a read-only authorization review of all 22 sections (0 BLOCKER / 0 HIGH / 0 MEDIUM / 1 LOW, every section PASS, the PR's 286-check matrix re-run on a real PostgreSQL by the reviewer and a 40-assertion probe of its own) and Codex's independent review — **FINAL VERDICT MERGE**; merged **`d69105c`** at 2026-09-22T16:51:03Z with `--match-head-commit` (the merge's second parent IS the reviewed SHA and the merged tree `9158716` is identical to the reviewed tree), merged-main CI green (Tests run 35756829735: compose-gate, test 3.11, test 3.12, ui-build; publish-agent run 35756830492: build-and-publish — all success) and the invariants re-run on merged main: 537 across the eight `test_a30_*` modules, full suite 4779 passed / 87 skipped, 86 real-PostgreSQL proofs (B0b's matrix on the real engine, S1/S2/S3/S4 incl. both S4 live upgrades, on alembic-managed databases), Console tsc 0 + Vitest 53/53, probe 39/40 with the one non-pass being the pre-existing empty-site guard recorded below. **The merge guard earned its keep:** the relayed verdict carried a one-character transcription of the SHA (`…cffe1e…` for `…cffefe…`), `--match-head-commit` refused it twice exactly as designed, and the merge landed only once the live head was typed back character for character. **F1 CLOSED.** A `device` or `device_class` grant satisfied `covers_device()` and `permits()` while contributing nothing to `site_ids`, which every device-bearing repository read filtered on — such a principal read ZERO list rows about devices the platform itself said it reaches. **One owner rule:** a row or subject that names a device is DEVICE-owned when that device CURRENTLY RESOLVES in the fleet cache AT THE ROW'S OWN SITE (S1's `AuthorizedTarget` rule for waves, now `FleetIndex`/`resolve_target` for one target), SITE-owned otherwise; readable under P when ONE effective grant carrying P covers the device by site, id or CURRENT class (`Grant.covers_device` asked of the target as it resolves — no second resolver, no inferred class, no contextual authority), which makes **R9 natural zero** (an unresolved device owns nothing and the grant is never revoked, mutated or made inert) and **R1 structural** (a blank class matches nothing; `read_reach` drops a blank ref so it can never become `IN ('')`). **Two named predicates, not one generic filter:** `scope_fleet_devices` (site ∪ id ∪ `lower(class)`) and `scope_device_owned(site_col, device_col)` (site OR a non-empty device column whose device resolves at that row's site by a correlated `EXISTS`, because only `cc_fleet_cache` carries `device_class`); both consume S2's `ReadReach` only, raise on a bare `ResolvedScope`, and for every tenant/org/site principal compile to the identical `site_id IN (…)` string they replaced — proven as SQL-string equality AND as byte-identical answers on 64 read paths × 6 personas with the predicates swapped for the legacy filter. **Device-owned (changed):** fleet cache (list, filtered+count, health counts, total), incidents (list, children, detail via one `visible_ids`), approval routes (queue + history, plain + paginated), agent proposals (queue, an agent's own list, receipts), outcome history, approval records (D8) and the single-target approval scope gate (`permits_owned`: R6 a matching class satisfies the SCOPE dimension only, every other gate mandatory; R9 an unresolved target is the site's). **Site-owned (unchanged, by decision):** audit (R3), campaigns/waves (R5), candidate skills (D4), learned signals + fleet patterns (S4's bands), the org tree (D3), the grant list, and `ResolvedScope.site_ids` (still truthful and permission-neutral). **Correlation never widens reach (R2/D7):** children filtered by the SAME predicate as the list — `children_of` was UNSCOPED on main, a latent cross-site read for site principals too; `parent_incident_id` null unless the parent is independently visible; `correlation` withheld from a reader who does not cover the site (a `network_ambiguity` row stores its PEERS' ids as `votes`); prior learning and the attention contract narrow site-scoped signals to cohort knowledge for such a reader, AFTER S4's `LearningView` has projected them. **Context is not authority (R10/D2):** `SiteRepo.list_all` stays AUTHORITATIVE (`sites_count`, reach, delegation, administration); `list_context` is a separate, permission-aware read whose four call sites are pinned by AST, projected as exactly `{id, site_name, contextual: true}` with `covers_site()` false, no field in either scope type that could hold it, and every mutation route swept against it (>30 routes, 403/404 only); Console's two authority surfaces (grant form, Organization) skip contextual rows. **Machine plane unchanged in shape:** `MACHINE_SURFACE` 13, ceiling `{fleet.view, incident.view, proposal.submit}`, no route reopens; a device-scoped agent's attention, incidents and own-proposal reads now agree with its canonical reach (live with a real credential). **What the live gate found on the way:** R4 was NOT true on a stack with real safety state (the unit estate had none), and P2 beside it — recorded, then CLOSED by S3 before this merged; the unit estate now carries safety state at every site so the assertion means something. **Unchanged, verified on merged main:** permissions (25, `harkeniq_console.permissions`), `ROLE_PERMISSIONS`, `MACHINE_PRINCIPAL_CEILING`, `MACHINE_SURFACE` (13), `ROUTE_CONTRACT` (98; 0 routes added), no migration (CC head `0027`, SM `0011`, Console `0004` carried from S4), `auth.py`/`route_contract.py`/`machine_identity.py`/`autonomy.py`/`learning_projection.py`/`receipts.py`/`generation_provenance.py`, the Site Manager and the agent untouched, `resolve()`, `permits()`, `read_reach`, S1, S2, S3, S4, the `server | switch` vocabulary; taxonomy, B0c, B1, B2 NOT started; **S3-E1/E2 ratified and NOT implemented** (A30.27). **Recorded as NON-BLOCKING follow-ups (Codex LOW), not fixed here:** (A) the pre-existing incident-detail guard `if row.site_id and …` lets an incident with an EMPTY `site_id` be read by any `incident.view` holder while the list hides it — no production writer produces the row (the poller writes the polled site's own id); hardening rule: a site-less incident is tenant-owned and fail-closed; (B) `governing_policy()` resolves the policy's `device_type` by `get_by_agent_id` (any site) while the scope gate resolves the target AT the subject's site — no authorization effect, a consistency cleanup; (C) the S4 real-PostgreSQL migration proofs need an alembic-managed database and fail with `relation "alembic_version" does not exist` on a `create_all` one — fixture skip-with-reason, never a production change; (D) D3, D6, D9, R7, D10, S2's composite projections and `/me`, S4's privacy/SQL-window items and `test_warranty.py`'s 2027-01-01 stand as recorded. 4719 → 4779 tests |
| 2026-09-22 | **S3-E1/E2/E3 architecture ratification CLOSED — Amendment A30.30** (decided: Vinod 2026-09-21 as R1–R7; closure recorded 2026-09-22 after general B0b merged and main-verified; DOCS ONLY — no production code, migration, route, permission or ceiling change; CC `0027` / SM `0011` / Console `0004`). A30.27 had recorded the package and sequenced its implementation after general B0b; with `d69105c` on main the ratification is closed durably so a later implementation cannot change the semantics silently — any deviation is a new dated amendment first. **S3-E1 target model:** SITE-LOCAL AUTONOMY ASSESSMENT (S3's primitive over the target's own site) + CLOSED GLOBAL SAFETY GATE (typed, members named by amendment before code) = FINAL EXECUTION ELIGIBILITY; the two are separate concepts; hidden state MUST NEVER increase machine authority (structural invariant; R5 removes the one non-monotone preflight input) and may reduce eligibility only through an explicitly ratified gate member; the gate's initial hidden-site-derived membership is EMPTY, so `error_budget_dropped_back` / `budget_window_exhausted` return to the target site and site-local drop-back/error-budget state stays site-local; target site not reported → REQUIRES_APPROVAL; target-site SM stop → local DENIED; a campaign is autonomous only if EVERY site in its immutable plan is locally autonomous AND the gate passes; scoped consumers see only the bounded typed reason `GLOBAL_SAFETY_CONSTRAINT` with no hidden identity/count/metric/reason; S1 unchanged; `fleet.view` stays the visibility basis. **S3-E2 dual evidence:** `decision_evidence_at_creation` immutable (never rewritten to a viewer's scope) and `viewer_projected_evidence` a read projection under CURRENT canonical reach — not a second decision record, not an authorization token; rationale rendered from typed facts so it cannot leak hidden historical/site evidence. **S3-E3:** A23 retained (cohort = tenant knowledge, site-scoped = scope-filtered); E3-F1 closed by S4, which stays canonical; A23 not reopened. **Status:** S3-E1 implementation NOT STARTED, S3-E2 NOT STARTED, S3-E3 redesign NOT REQUIRED. **Sequence:** A6-4B0c → A6-4B1 → A6-4B2, none started; B0c boundary preview recorded (meter every explicit machine route, completeness anchored on `MACHINE_SURFACE` not a router prefix, Attention + Incidents covered, structurally hard to add an unmetered machine route, no permission / ceiling / route, A6-4A narrowing and S1–S4/B0b preserved). Follow-ups preserved non-blocking (A/B/C from PR #48, the S4 §34i items, the `test_directives.py` hang, Console/PostgreSQL CI coverage, D3/D6/D9/R7/D10, warranty 2027-01-01). Design §34k |
| next | **A6-4B0c Machine Read Metering Completion, then A6-4B1, then A6-4B2.** The S3-E1/E2/E3 architecture is RATIFIED and CLOSED (A30.30, design §34k, 2026-09-22): S3-E1 and S3-E2 implementation NOT started, S3-E3 needs no redesign; they follow B0c → B1 → B2 in their own slice with their own boundary and review, unless a proven fail-open needs containment first. **B0c must** meter every explicit machine-readable route, anchor completeness to `MACHINE_SURFACE` / the canonical route contract (never a router-prefix assumption), cover Attention and Incidents, make adding a machine route without a metering decision structurally difficult, add no permission, no machine-ceiling capability and no new machine route, and preserve A6-4A's narrowing and S1/S2/S3/S4/B0b. **First** A6-4B0c (A30.12: served-read metering on the three off-router machine routes, and the completeness guard re-anchored from one router prefix onto `MACHINE_SURFACE` itself so it fails when any future machine route lacks a decided meter answer; a correctly-bound runtime can receive 429 for the first time, so the window is set against real gate traffic before it lands) → A6-4B1 (A30.13: governed capability + parameter discovery, the additive half A29.11 separated from the narrowing — composing `load_capability_registry`, `capabilities.parameter_contract`, `catalogue_view`, `ACTION_RISK` and `ACTION_REVERSIBILITY` with **no second catalogue**, ADDRESSABLE reported apart from AUTHORIZED TO EXECUTE; `/api/autonomy/` and `/api/capabilities/*` stay HUMAN until it lands) → A6-4B2 (A30.14: operational context projection — attention/incident machine projections and the self-scope reach B7 asks for, with its own trust semantics). Each slice merges and main-verifies before the next begins; **they are not combined**, and each receives its own boundary and ratification first, with the A25.9 recursive sweep as its own review. **Then** the PX program ratified in A28 — PX1A (Console Vitest in CI, complete token architecture, semantic status module, canonical terminology, token lint, a11y tooling baseline, structural UI test foundation) → PX1B (brand and visual foundation) → PX2 (shell, IA, Attention operational home, context bar, responsive foundation) → PX3A/B/C/D → PX4A/B → PX5A/B → PX6 → PX7 → PX8. Then A6-5 event-driven interaction / subscriptions / webhooks, A6-6 MCP adapter over the canonical governed plane (A22.1: thin adapters, never a second authority), SDK/API ergonomics, and — separately, with its own evidence-trust model (A24.10) — trusted capability-intent/evidence ingress. Platform Operations (governed support workflows / customer-authorised break-glass, never by weakening A12.1). Also: the §11 autonomy-half decision for the unmapped classes when Vinod calls it; trained predictive models once outcome data accumulates; vendor NOS breadth (Arista/Cisco) / server NIC collection (TODOS N1). Informational, tracked and NOT blockers: Console Vitest is not executed in CI (PX1A closes it); the Operational Agent detail page couples ingress with other reads in one `Promise.all`; the A23-2 gate step rewinds a destructive migration with Central Command running; a schema-invalid submission body is refused before the per-agent attempt ledger (bounded by the 16KiB pre-parse ceiling); proposal admission locks per tenant, favouring correctness over same-tenant throughput; cursor pagination for the proposal LIST deferred; `AgentProposalRepo.find_open` has zero callers; `test_http_health_endpoint` is port-bound and load-sensitive; `/api/scope-grants/me` over-predicts reads for a mixed-grant principal and the composite projections follow their route's permission (both recorded in the S2 row); the Site Manager outcome-identity fallback hardening (D10, not a prerequisite); the PostgreSQL integration proofs are DSN-gated and not run in CI; **from PR #48's review (Codex LOW / follow-ups, NON-BLOCKING):** (A) the pre-existing `GET /api/incidents/{id}` guard reads an EMPTY-`site_id` incident to any `incident.view` holder while the list hides it — no production writer produces the row; hardening rule: site-less is tenant-owned and fail-closed; (B) `governing_policy()` device-type resolution by `get_by_agent_id` vs the scope gate's at-site `resolve_target` — consistency only; (C) the S4 PostgreSQL migration proofs need an alembic-managed database (fixture skip-with-reason); **from S4 (§34i), preserved:** candidate identity strengthening (`skill_id` alone never proves content equivalence), the fixed pre-projection SQL windows (patterns 1000 / signals 500 / cycles 200), minute-boundary timestamp residue, `observation_count` semantics at minute grain, predictive `cohort_failure_rate` / `outcomes_considered`, the live gate not exercising a machine token on `/api/attention/`, and the intermittent `test_directives.py` teardown hang (infrastructure, root cause open) |

## Locked engineering decisions

- **Stack:** Python/FastAPI + SQLAlchemy + PostgreSQL (TimescaleDB for telemetry) for all
  server-side services; React + TypeScript consoles; existing compiled gRPC proto reused.
- **Identity:** self-hosted Keycloak — one realm per tenant + a platform realm; same
  pattern runs air-gapped. 7 fixed roles + tenant custom roles (spec §4).
- **Billing:** in-house ledger-first core; `PaymentProvider` adapter interface with
  Razorpay (India/INR) + Stripe (US/EU) routed by tenant region; annual node commit +
  monthly high-water true-up; delinquency = grace → console restriction → manual suspend
  (spec §5).
- **Never remotely disable on-prem agents or Site Managers** — not for non-payment, not
  for anything (R-H7). Diagnosis is safety infrastructure.
- **Payment-code rigor:** zero compromise; implement gateway behavior only after reading
  current Razorpay/Stripe docs; idempotent verified webhooks; no card data stored.
- **Denied actions are final** (D16); approvals per action class, always audited.
- Sovereign/air-gapped deployment is a requirement, not an edge case (doc 01 §7):
  L1–L3 stay single-tenant software; tenancy lives only at L4.
- Open questions live in spec §8 with an owning slice — answer there, don't re-ask ad hoc.

## Dev conventions

- Tests: `cd /home/vinod/HarkenIQ && source .venv/bin/activate && PYTHONPATH= python -m pytest tests/ -q`
  (bare `python` is not on PATH; always use the venv).
- Code layout: agent under `src/harkeniq/`; new services land as `services/site_manager/`,
  `services/central_command/`, `services/console/` (backend) + `console-ui/` (React).
- Full suite green before any slice lands. Milestone commits/pushes only on explicit
  user approval; commit style follows `git log` (short imperative subject).
- Suggestions are Claude's; **product decisions are Vinod's** — surface options, ask
  before assuming.

## Skill routing

When the user's request matches an available skill, invoke it via the Skill tool. When in doubt, invoke the skill.

Key routing rules:
- Product ideas/brainstorming → invoke /office-hours
- Strategy/scope → invoke /plan-ceo-review
- Architecture → invoke /plan-eng-review
- Design system/plan review → invoke /design-consultation or /plan-design-review
- Full review pipeline → invoke /autoplan
- Bugs/errors → invoke /investigate
- QA/testing site behavior → invoke /qa or /qa-only
- Code review/diff check → invoke /review
- Visual polish → invoke /design-review
- Ship/deploy/PR → invoke /ship or /land-and-deploy
- Save progress → invoke /context-save
- Resume context → invoke /context-restore
- Author a backlog-ready spec/issue → invoke /spec
